\documentclass[a4paper,11pt]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[english]{babel}
\usepackage[margin=2.4cm]{geometry}
\usepackage{tikz}
\usepackage{listings}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{booktabs}
\usepackage{caption}
% Captions in this report are long and explanatory; turn off the single line
% check so the caption package never tries to typeset a multi-thousand character
% caption on one measured line (which overflows TeX's max dimension).
\captionsetup{singlelinecheck=false}
\usepackage{float}
\usepackage{array}
\usepackage{rotating} % sidewaysfigure for the wide board schematic

\usetikzlibrary{arrows.meta, positioning, shapes.geometric, calc, fit, decorations.pathreplacing, decorations.pathmorphing, backgrounds, automata, shapes.misc, shapes.arrows, patterns}

\hypersetup{
  colorlinks=true,
  linkcolor=black,
  urlcolor=black,
  citecolor=black
}

\lstset{
  basicstyle=\ttfamily\footnotesize,
  breaklines=true,
  frame=single,
  rulecolor=\color{black},
  numbers=left,
  numberstyle=\tiny\color{black!55},
  numbersep=6pt,
  showstringspaces=false,
  keywordstyle=\bfseries,
  commentstyle=\color{black!55}\itshape,
  stringstyle=\color{black!75}
}

% Whole report is black and white: every accent colour is redefined to black
% (so the !N tints become grey shades) and nothing prints in colour.
\definecolor{boxbg}{gray}{1}
\definecolor{boxstroke}{gray}{0}
\definecolor{busblue}{gray}{0}
\definecolor{accentred}{gray}{0}
\definecolor{accentgreen}{gray}{0.4}

\title{\textbf{EFES Master Node:\\a Full Custom FPGA plus ESP32 Implementation\\of the AUSP Acoustic Protocol}}
\author{Gioele Giunta \\ \texttt{s360501} \\[4pt] \small Politecnico di Torino, LM 32 Computer Engineering \\ \small Course: Electronics for Embedded Systems}
\date{Academic Year 2025 / 2026}

\begin{document}

\maketitle
\thispagestyle{empty}

\noindent\textbf{Note on this report.} I proposed this project to the professor as an
alternative to the written exam, and the professor approved the idea. So this is
not a lab assignment: it is a personal project that I designed and built on my
own, and what follows is the report I am sending to him. For this reason the text
is written in the first person. The focus is the master node, that is the FPGA
side plus the ESP32 that drives it.

\tableofcontents
\newpage

% =====================================================================
\section{Introduction}
% =====================================================================

The AUSP protocol is a communication protocol that I created for my Bachelor
thesis and later submitted to the IEEE IoT Magazine under the title
\emph{Design and Experimental Validation of a Low-Cost Acoustic Communication
Protocol for Underground Sensor Networks}. The goal of the protocol is to let
two devices talk to each other using only very cheap hardware, namely a speaker
and a microphone. The protocol sends packets by emitting pairs of superimposed
tones: at every step one tone identifies the logical channel (the carrier) and a
second tone carries the data symbol.

On the receiver side an FFT moves the microphone signal from the time domain to
the frequency domain, so that the pair of active frequencies can be read out as
peaks in the spectrum. To keep the detection reliable in the presence of noise I
had implemented, in the original thesis, several techniques: a machine learning
stage that learns the noise floor and the low frequency roll-off of the cheap
microphone (electret capsules lose sensitivity at low frequency), and a parabolic
interpolation that refines the position of each spectral peak to better than one
bin.

\subsection{What this version is}

The version documented here is a full custom FPGA implementation of the
\emph{master} side of my protocol, paired with an ESP32 in a hybrid arrangement.
The FPGA is a Tang Nano 20K (Gowin GW2AR-18). I used Gowin EDA for synthesis,
place and route and programming, and GHDL to simulate my VHDL before putting it
on the board.

On the FPGA I preferred to write the interconnect, the peripherals and the memory
controller myself in VHDL, and to drive their registers from a soft core CPU so
that all the high level logic lives in software. I used only three IP cores from
Gowin:

The \textbf{PicoRV32} soft core is the CPU that runs the firmware and drives every
peripheral over the on chip bus. The \textbf{Gowin rPLL} takes the 27\,MHz board
crystal and produces the two synchronous clocks the design needs (40.5\,MHz for
the bus and the CPU, and a phase shifted copy of it for the SDRAM die).
The \textbf{Gowin FFT} core sits inside the hardware accelerator that I built
around it to turn microphone samples into a spectrum.

Everything else on the FPGA is custom VHDL that I wrote: a generic UART module
(instantiated twice, for the protocol characters and for the storage commands), a
generic SPI byte engine used to talk to the NOR flash, a second SPI master
specialised and timed for receiving audio from the ADC, a third, transmit only
SPI that drives a small TFT display, a generic GPIO peripheral, and a PWM
peripheral that generates the output tones for the speaker.

On the ESP32 side, the board connects to the FPGA through two independent UART
links, so four wires in total (two TX and two RX), accessed at register level.
The ESP32 runs the AUSP protocol logic: it queues the inbound and outbound
symbols, rebuilds the packets, and decides which packet to send next. The ESP32
is also connected to the internet and exchanges commands with a web dashboard over
a WebSocket. In this arrangement the FPGA is the interface that lets the system
speak my acoustic protocol, and the ESP32 is the brain that runs the protocol on
top of it.

\subsection{The hardware I used}

For persistent storage I used a W25Q64 NOR flash (64\,Mbit, 8\,MByte). The local
display is a 2 inch IPS TFT, a GMT020-02-7P with the usual ST7789 controller,
$240\times320$, driven over SPI. The
microphone is an ordinary electret condenser capsule. Its signal is amplified by
an MCP6022 operational amplifier and digitised by an MCP3201 12-bit ADC, which the
FPGA reads over SPI. The speaker is an ordinary 8\,$\Omega$ speaker driven from
the PWM output through a low side MOSFET. Power comes from a single 12\,V supply
that a DFRobot DFR1202 buck module steps down to 5\,V for the Tang Nano
\texttt{VIN}; the 12\,V rail also directly feeds the speaker stage. Several
\texttt{IRLZ44N}/\texttt{IRLZ44NPBF} MOSFETs do the switching, plus the usual
capacitors and diodes.

The slave device, the one that receives orders over the air from this master, is
much simpler and is documented elsewhere: it is an ESP32 wired at register level
to an I2S microphone on one side and, through I2S again, to a class D amplifier
and a speaker on the other.

% =====================================================================
\section{System overview}
% =====================================================================

The system is a two node star. The master is the hub, the slave is the single
spoke, and a web dashboard opened in any browser is an optional viewer.
Figure~\ref{fig:system} shows the picture.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    every node/.style={font=\small},
    box/.style={draw=boxstroke, fill=boxbg, rounded corners=2pt, minimum height=1.1cm, align=center, inner sep=4pt},
    relay/.style={draw=boxstroke, fill=boxbg, rounded corners=14pt, minimum width=2.6cm, minimum height=1.2cm, align=center},
    audio/.style={->, line width=1.1pt, accentred, decorate, decoration={snake, amplitude=1pt, segment length=4pt}},
    wifi/.style={->, line width=0.9pt, busblue, dashed}
]
\node[box, minimum width=4.2cm] (master) at (0,0) {%
    \textbf{Master node}\\[2pt]
    \footnotesize Tang Nano 20K (FPGA) + ESP32\\[1pt]
    \footnotesize W25Q64 NOR flash, mic, speaker
};
\node[box, minimum width=3.8cm] (slave) at (10,0) {%
    \textbf{Slave node}\\[2pt]
    \footnotesize ESP32, I2S mic + class D speaker
};
\node[relay] (cloud) at (5, 3) {Cloudflare\\ Worker\\ (WebSocket relay)};
\node[box, minimum width=2.6cm] (dash) at (5, 5.2) {%
    \textbf{Dashboard}\\[1pt]
    \footnotesize browser
};
\draw[audio] ([yshift=4pt]master.east) -- node[above, yshift=2pt, accentred, font=\footnotesize] {tones, master carrier 2000 Hz} ([yshift=4pt]slave.west);
\draw[audio] ([yshift=-4pt]slave.west) -- node[below, yshift=-2pt, accentred, font=\footnotesize] {tones, slave carrier 2400 Hz} ([yshift=-4pt]master.east);
\draw[wifi] (master.north) |- (cloud.south west);
\draw[wifi] (cloud.north) -- (dash.south);
\node[busblue, font=\footnotesize] at (2.2, 2.0) {Wi\,Fi};
\end{tikzpicture}
\caption{System level view. The only link between master and slave is acoustic
(wavy arrows). The Wi\,Fi link only carries dashboard traffic; the slave does
not need it.}
\label{fig:system}
\end{figure}

The acoustic link is the only path between master and slave. There is no shared
cable, no Bluetooth, no I2C. Everything the two say to each other, presence
requests, identification, acknowledgements, abort, has to be encoded into tones
and demodulated on the other side (Section~\ref{sec:protocol}). The Wi\,Fi link is
only there so I can watch and command the system from a browser; it is not part of
the acoustic protocol.

Inside the master, three units split the work: the FPGA fabric, the soft core (a
PicoRV32 inside the FPGA), and the ESP32. The fabric does everything that is timing
critical or sample rate bound: it reads the ADC, computes the FFT, copies the spectrum into an on chip snapshot, generates the output PWM tone, and shifts the raw
SPI frames to the NOR flash and the TFT. The soft core does the per frame work that
is not sample bound: it reads the spectrum snapshot over the bus, decodes the tones,
decides whether the channel is free before emitting, runs the W25Q storage protocol
and draws the display. The ESP32 does everything that is decision bound: it
runs the protocol state machine, talks to the dashboard, and tells the soft core
which tones to emit. Table~\ref{tab:partition} lists the split operation by operation.
The units are joined by two UARTs, one carrying the decoded and to be emitted acoustic
symbols, the other carrying the flash storage commands.

\begin{table}[H]
\centering
\caption{Hardware/software partition: who performs each operation. \emph{Fabric} is
fixed FPGA logic, \emph{soft core} is C running on the on chip PicoRV32, \emph{ESP32}
is the companion microcontroller. The fabric only samples, transforms, moves, emits
and stores; every decision (decode, channel sense, protocol) runs in software.}
\label{tab:partition}
\begin{tabular}{lll}
\toprule
\textbf{Operation} & \textbf{Where} & \textbf{Type} \\
\midrule
ADC sampling (MCP3201, SPI)              & FPGA fabric        & hardware \\
Move samples to SDRAM (DMA)              & FPGA fabric        & hardware \\
512 point FFT                           & FPGA fabric        & hardware \\
Store spectrum in SDRAM                  & FPGA fabric        & hardware \\
Copy spectrum to BSRAM snapshot          & FPGA fabric        & hardware \\
Two tone generation (DDS, sine sum)      & FPGA fabric        & hardware \\
NOR flash storage (W25Q protocol)        & soft core          & software \\
TFT dashboard rendering                  & soft core          & software \\
Read snapshot over the Wishbone bus      & soft core          & software \\
Tone decode (carrier + bit)              & soft core          & software \\
Carrier sense, emit gating (CSMA)        & soft core          & software \\
Choose which tones to emit               & soft core          & software \\
Protocol state machine                   & ESP32              & software \\
Pattern $\leftrightarrow$ message framing & ESP32, soft core  & software \\
Wi\,Fi and dashboard                     & ESP32              & software \\
\bottomrule
\end{tabular}
\end{table}

\subsection{The board around the FPGA}
\label{sec:board}

The SoC of the next sections lives \emph{inside} the FPGA, but the master node is
a whole board: a power chain that ends in the FPGA, and a ring of real parts
around it (storage, display, indicator LEDs, the analog microphone front end and
the speaker driver). Figure~\ref{fig:board} is the board level schematic with the
actual components I used. Three supply rails organise it. The 12\,V comes from an
Apple USB-C charger through a USB-C \emph{trigger} module (it negotiates a fixed
12\,V over PD/QC/AFC, so the charger behaves like a plain 12\,V brick); that 12\,V
feeds both a DFR1202 buck converter, which steps it down to the 5\,V the Tang Nano
20K wants on \texttt{VIN}, and the speaker, which is loud enough to run straight
off 12\,V. The FPGA board then exposes two rails of its own: \textbf{3.3\,V} for
the digital peripherals and \textbf{5\,V} (the \texttt{VIN} passed through) for the
analog front end. One detail that bit me and is now load bearing: the 3.3\,V does
not go straight to the peripherals, it passes through an \texttt{IRLZ44N} MOSFET
that the GPIO (pin 80) holds on, so the GPIO output \emph{must} stay at \texttt{1}
or the NOR flash, the display and the LEDs all lose their supply
(Section~\ref{sec:soc}).

\begin{figure}[p]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
    every node/.style={font=\footnotesize},
    box/.style={draw, line width=0.8pt, fill=white, align=center, inner sep=3pt},
    chip/.style={draw, line width=0.9pt, fill=white, align=center},
    wire/.style={line width=0.55pt},
    pwr/.style={line width=1.1pt},
    lab/.style={font=\scriptsize, fill=white, inner sep=0.7pt},
    pin/.style={font=\scriptsize, inner sep=0.7pt},
    nb/.style={draw, rounded corners=1pt, fill=white, inner sep=1.3pt, font=\scriptsize\sffamily}
]
\newcommand{\gnd}[1]{\draw[wire] #1 -- ++(0,-0.2) coordinate(gg); \draw[wire] ($(gg)+(-0.2,0)$)--($(gg)+(0.2,0)$); \draw[wire] ($(gg)+(-0.12,-0.07)$)--($(gg)+(0.12,-0.07)$); \draw[wire] ($(gg)+(-0.05,-0.14)$)--($(gg)+(0.05,-0.14)$);}
\newcommand{\capd}[3]{\draw[wire] (#1-0.2,#2)--(#1+0.2,#2); \draw[wire] (#1-0.2,#2-0.14)--(#1+0.2,#2-0.14); \node[lab,anchor=west] at (#1+0.26,#2-0.07){#3};}
\newcommand{\rh}[4]{\draw[wire,decorate,decoration={zigzag,segment length=2.8pt,amplitude=2.5pt}] (#1,#3)--(#2,#3); \node[lab] at ({(#1+#2)/2},#3+0.3){#4};}
\newcommand{\rv}[4]{\draw[wire,decorate,decoration={zigzag,segment length=2.8pt,amplitude=2.5pt}] (#1,#2)--(#1,#3); \node[lab,anchor=west] at (#1+0.18,{(#2+#3)/2}){#4};}
\newcommand{\dotj}[1]{\fill #1 circle (1.9pt);}
\newcommand{\mos}[3]{\draw[wire] (#1-0.66,#2-0.5) rectangle (#1+0.66,#2+0.5); \node[font=\tiny,align=center] at (#1,#2){#3}; \node[lab,anchor=east] at (#1-0.68,#2){G}; \node[lab,anchor=south] at (#1,#2+0.52){D}; \node[lab,anchor=north] at (#1,#2-0.52){S};}
\newcommand{\led}[2]{\draw[wire] (#1-0.22,#2+0.22)--(#1+0.22,#2+0.22)--(#1,#2-0.15)--cycle; \draw[wire] (#1-0.22,#2-0.15)--(#1+0.22,#2-0.15); \draw[-{Stealth[length=3pt]},line width=0.4pt] ($(#1+0.25,#2+0.11)$)--($(#1+0.45,#2+0.31)$);}

% ===== POWER CHAIN =====
\node[box,minimum width=2.0cm,minimum height=0.9cm] (psu)  at (1.6,22.0){Apple\\USB-C PSU};
\node[box,minimum width=2.6cm,minimum height=0.9cm] (pd)   at (4.5,22.0){USB-C PD/QC/AFC\\trigger};
\node[box,minimum width=2.5cm,minimum height=0.9cm] (buck) at (7.7,22.0){Buck \texttt{DFR1202}\\12\,V$\to$5\,V};
\draw[pwr] (psu.east)--(pd.west);
\draw[pwr] (pd.east)-- node[lab,above]{12\,V} (buck.west);
\draw[pwr] (buck.east)--(9.2,22.0); \node[nb,anchor=west] at (9.25,22.0){5\,V};
\coordinate (p12) at ($(pd.east)!0.5!(buck.west)$); \draw[pwr] (p12)--(6.1,21.2); \node[nb] at (6.1,21.35){12\,V};

% ===== ESP32 =====
\node[box,minimum width=2.4cm,minimum height=1.5cm] (esp) at (12.9,20.0){\textbf{ESP32}\\UART1 + UART2\\Wi-Fi};
\draw[-{Stealth[length=5pt]},wire] (esp.north)--++(0,0.45) node[lab,anchor=south]{Wi-Fi $\to$ dashboard};

% ===== FPGA =====
\node[chip,minimum width=5.4cm,minimum height=11.4cm] (fpga) at (7.0,13.2){};
\node[align=center] at (7.0,13.6){\textbf{Tang Nano 20K}\\Gowin GW2AR-18\\[2pt]\emph{SoC inside}};
% TOP pins
\foreach \x/\t/\c in {5.2/{VIN}/p_vin, 6.3/{17}/p_ctx, 6.8/{18}/p_crx, 7.7/{72}/p_ntx, 8.2/{20}/p_nrx}{
  \coordinate (\c) at (\x,18.9); \draw[wire] (\x,18.9)--(\x,19.15); \node[pin,anchor=south,rotate=90] at (\x,19.2){\t};}
\node[pin] at (7.25,19.95){UARTs $\to$ ESP32};
% RIGHT pins
\foreach \y/\t/\c in {18.3/{3.3V}/p_33, 17.7/{80}/p_gpio,
                      16.45/{41}/p_fcs, 16.0/{48}/p_fmiso, 15.55/{42}/p_fsck, 15.1/{51}/p_fmosi,
                      12.4/{27}/p_tscl, 12.0/{28}/p_tsda, 11.6/{29}/p_tdc, 11.2/{30}/p_trst, 10.8/{31}/p_tcs}{
  \coordinate (\c) at (9.7,\y); \draw[wire] (9.7,\y)--(9.95,\y); \node[pin,anchor=west] at (10.0,\y){\t};}
% BOTTOM pins
\foreach \x/\t/\c in {4.9/{5V}/p_5out, 5.7/{74}/p_acsk, 6.2/{73}/p_acs, 6.7/{85}/p_amiso, 7.2/{75}/p_amosi, 8.1/{76}/p_spwm, 8.9/{77}/p_lpwm}{
  \coordinate (\c) at (\x,7.5); \draw[wire] (\x,7.5)--(\x,7.25); \node[pin,anchor=north,rotate=90] at (\x,7.2){\t};}
\node[pin,anchor=north] at (6.45,6.5){ADC SPI};

% ===== VIN from buck =====
\draw[pwr] (buck.south)--(7.7,20.6)--(5.2,20.6)--(p_vin);
% ===== UARTs -> ESP32 =====
\draw[wire] (p_ctx)--(6.3,19.4)--(11.7,19.4);
\draw[wire] (p_crx)--(6.8,19.6)--(11.7,19.6);
\draw[wire] (p_ntx)--(7.7,19.8)--(11.7,19.8);
\draw[wire] (p_nrx)--(8.2,20.0)--(11.7,20.0);
\node[lab] at (10.0,19.45){char 17/18}; \node[lab] at (10.0,20.05){NOR cmds 72/20};

% ===== 3.3 V gate + 3.3Vsw rail =====
\mos{10.9}{18.3}{\texttt{IRLZ44N}}
\draw[pwr] (p_33)--(9.7,19.0)--(10.9,19.0)--(10.9,18.8);  % 3.3V -> D
\draw[wire] (p_gpio)--(10.25,17.7)--(10.25,18.3);         % gpio -> G
\node[lab,anchor=south] at (9.9,17.45){held \texttt{1}};
\draw[pwr] (10.9,17.8)--(10.9,17.4)--(14.6,17.4); \node[nb,anchor=south] at (13.2,17.45){3.3\,V\textsubscript{sw}};
\draw[pwr] (14.6,17.4)--(14.6,6.5);  % rail verticale 3.3Vsw

% ===== NOR flash =====
\node[chip,minimum width=2.1cm,minimum height=2.3cm] (nor) at (12.4,15.6){\textbf{W25Q64}\\NOR\\flash};
\foreach \y/\t/\c in {16.45/{1 /CS}/n_cs, 16.0/{2 DO}/n_do, 15.55/{6 CLK}/n_clk, 15.1/{5 DI}/n_di}{
  \coordinate (\c) at (11.35,\y); \draw[wire] (11.35,\y)--(11.1,\y); \node[pin,anchor=east] at (11.05,\y){\t};}
\foreach \y/\t/\c in {16.45/{8 VCC}/n_vcc, 16.0/{7 /HOLD}/n_hold, 15.55/{3 /WP}/n_wp, 15.1/{4 GND}/n_gnd}{
  \coordinate (\c) at (13.45,\y); \draw[wire] (13.45,\y)--(13.7,\y); \node[pin,anchor=west] at (13.75,\y){\t};}
\draw[wire] (p_fcs)--(n_cs);
\draw[wire] (p_fmiso)--(n_do);
\draw[wire] (p_fsck)--(n_clk);
\draw[wire] (p_fmosi)--(n_di);
\draw[pwr] (n_vcc)--(14.6,16.45); \dotj{(14.6,16.45)} \node[nb,anchor=south] at (14.1,16.5){3.3\,Vsw}; % VCC -> rail
\draw[wire] (n_hold)--(14.2,16.0)--(14.2,16.45);                              % /HOLD high
\draw[wire] (n_wp)--(14.05,15.55)--(14.05,16.45);                            % /WP high
\gnd{(n_gnd)}
% condensatori in parallelo sulla VCC
\draw[wire] (13.45,16.45) -- (13.45,16.95); \capd{13.45}{16.95}{10\,$\mu$F} \gnd{(13.45,16.75)}
\draw[wire] (13.45,16.45) -- (12.85,16.45) -- (12.85,16.95); \capd{12.85}{16.95}{1\,$\mu$F} \gnd{(12.85,16.75)}

% ===== TFT display =====
\node[chip,minimum width=2.1cm,minimum height=2.6cm] (tft) at (12.4,11.4){\textbf{GMT020}\\\texttt{-02-7P}\\ST7789};
\foreach \y/\t/\c in {12.4/{SCL}/t_scl, 12.0/{SDA}/t_sda, 11.6/{DC}/t_dc, 11.2/{RST}/t_rst, 10.8/{CS}/t_cs}{
  \coordinate (\c) at (11.35,\y); \draw[wire] (11.35,\y)--(11.1,\y); \node[pin,anchor=east] at (11.05,\y){\t};}
\draw[wire] (p_tscl)--(t_scl);
\draw[wire] (p_tsda)--(t_sda);
\draw[wire] (p_tdc)--(t_dc);
\draw[wire] (p_trst)--(t_rst);
\draw[wire] (p_tcs)--(t_cs);
\draw[pwr] (13.45,12.4)--(14.6,12.4); \dotj{(14.6,12.4)} \node[pin,anchor=south] at (13.9,12.45){VCC};
\draw[wire] (13.45,10.9)--(13.8,10.9); \node[pin,anchor=west] at (13.85,10.9){GND}; \gnd{(13.85,10.9)}
% condensatore di bypass direttamente sul pin VCC
\draw[wire] (13.45,12.4) -- (13.45,12.9); \capd{13.45}{12.9}{1\,$\mu$F} \gnd{(13.45,12.7)}

% ===== LEDs + speaker drivers =====
\draw[pwr] (14.6,9.4)--(11.5,9.4); \dotj{(14.6,9.4)} \draw[pwr] (11.5,9.4)--(11.5,8.7);
\node[pin,anchor=west] at (11.55,9.1){3.3\,V\textsubscript{sw}};
\rv{11.5}{8.7}{8.0}{$2\times$R}
\led{11.5}{7.3} \draw[wire] (11.5,8.0)--(11.5,7.52); \draw[wire] (11.5,7.15)--(11.5,6.8);
\node[pin,anchor=west] at (11.7,7.3){2 LEDs};
\mos{11.5}{5.9}{\texttt{IRLZ44N}} \draw[wire] (11.5,6.8)--(11.5,6.4);
\draw[wire] (p_lpwm)--(8.9,6.8)--(10.85,6.8)--(10.85,5.9); \node[lab,anchor=south] at (9.9,6.85){pwm\_4 (77)};
\gnd{(11.5,5.35)}
% 12 V supply per speaker – percorso pulito a sinistra
\draw[pwr] (6.1,21.2)--(1.5,21.2)--(1.5,6.0)--(14.0,6.0)--(14.0,7.3);
\node[lab,anchor=east] at (13.95,6.7){12\,V};
\draw[wire] (13.7,7.3) rectangle (14.3,6.7); \draw[wire] (14.3,7.3)--(14.7,7.6)--(14.7,6.4)--(14.3,6.7);
\node[pin,anchor=north] at (14.2,6.65){8\,$\Omega$ spk};
\draw[wire] (14.0,6.7)--(14.0,6.4); \mos{14.0}{5.9}{\texttt{IRLZ44N}} \draw[wire] (14.0,6.4)--(14.0,6.4);
\draw[wire] (p_spwm)--(8.1,6.5)--(13.4,6.5)--(13.4,5.9); \node[lab,anchor=south] at (10.6,6.55){pwm\_10 (76)};
\gnd{(14.0,5.35)}

% ===== 5 V analog rail =====
\draw[pwr] (p_5out)--(4.9,6.8)--(0.6,6.8)--(0.6,1.1); \node[nb] at (0.6,5.6){5\,V analog};

% ===== microfono + AC coupling =====
\draw[wire] (1.3,3.6) circle (0.3); \node[pin] at (1.3,3.6){MIC}; \node[pin,anchor=south] at (1.3,4.0){electret};
\draw[pwr] (0.6,4.0)--(1.3,4.0); \dotj{(0.6,4.0)} \gnd{(1.3,3.28)}
\draw[wire] (1.65,3.6)--(2.05,3.6);
\draw[wire] (2.15,3.35)--(2.15,3.85); \draw[wire] (2.32,3.35)--(2.32,3.85); \node[lab,anchor=south] at (2.24,3.85){1\,$\mu$F};
\draw[wire] (2.32,3.6)--(2.7,3.6); \rh{2.7}{3.6}{3.6}{100\,$\Omega$}
\draw[wire] (3.6,3.6)--(4.15,3.6);

% ===== bias divider 47k/47k -> 2.5 V (spostato a sx) =====
\draw[pwr] (0.3,5.3) -- (0.3,5.3) node[above]{}; % dummy
\draw[pwr] (0.6,5.3)--(0.3,5.3); \dotj{(0.6,5.3)}
\rv{0.3}{5.3}{4.5}{47\,k} \dotj{(0.3,4.5)} \node[nb,anchor=east] at (0.1,4.5){2.5\,V};
\rv{0.3}{4.5}{3.7}{47\,k} \gnd{(0.3,3.7)}
% collegamento del bias al nodo di R1
\draw[wire] (0.3,4.5) -- (1.05,4.5) -- (1.05,3.7) -- (2.0,3.7) -- (3.1,3.7);

% ===== MCP6022 op‑amp =====
\draw[wire,fill=white] (4.45,4.2)--(4.45,2.6)--(6.25,3.4)--cycle;
\node[font=\scriptsize] at (4.9,3.4){\textbf{MCP6022}};
\node[pin] at (4.68,3.7){$-$}; \node[pin] at (4.68,3.1){$+$};
\coordinate (opp) at (4.45,3.1); \coordinate (opm) at (4.45,3.7); \coordinate (opo) at (6.25,3.4);
\draw[wire] (4.15,3.6)--(4.15,3.1)--(opp); \dotj{(4.15,3.1)} \node[pin,anchor=north] at (4.05,3.0){3};
% rete di retroazione – resistenze spaziate
\rh{2.6}{3.5}{2.8}{R\textsubscript{b} 100\,k}
\draw[wire] (4.15,3.1)--(4.15,2.8)--(3.5,2.8);
\draw[wire] (opm)--(4.05,3.7); \node[pin,anchor=south] at (4.0,3.75){2};
\rh{3.1}{4.05}{3.7}{R1 10\,k}
\draw[wire] (4.05,3.7)--(4.05,2.4); \rh{4.05}{6.55}{2.4}{R2 100\,k}
\draw[wire] (6.55,2.4)--(6.9,2.4)--(6.9,3.4)--(opo); \dotj{(6.9,3.4)}
\draw[pwr] (0.6,4.0)--(5.1,4.0)--(5.1,3.95); \dotj{(0.6,4.0)} \node[pin,anchor=west] at (5.15,3.68){8 VDD 5\,V};
\draw[wire] (5.1,2.85)--(5.1,2.45); \node[pin,anchor=north] at (5.1,2.4){4 VSS}; \gnd{(5.1,2.4)}
\node[pin,anchor=south] at (6.5,3.48){1 VOUTA};

% ===== LPF + MCP3201 =====
\draw[wire] (6.9,3.4)--(7.3,3.4); \rh{7.3}{8.3}{3.4}{R\textsubscript{f} 1.1\,k}
\draw[wire] (8.3,3.4)--(9.0,3.4);
\draw[wire] (8.6,3.4)--(8.6,2.9); \capd{8.6}{2.9}{C\textsubscript{f} 10\,nF} \gnd{(8.6,2.7)}
\node[lab,anchor=south] at (7.8,3.7){LPF $f_c\!\approx\!14.5$\,kHz};
\node[chip,minimum width=2.4cm,minimum height=2.0cm] (adc) at (9.9,2.9){\textbf{MCP3201}\\12-bit SAR};
% IN+
\draw[wire] (9.0,3.4) -- (8.7,3.4); \node[pin,anchor=south east] at (8.95,3.45){IN+ 2};
% VREF / VSS (percorso alto, fuori dall'op‑amp)
\draw[pwr] (0.6,4.8) .. controls (4,4.8) and (7,4.8) .. (8.7,4.8) -- (8.7,3.2); \dotj{(0.6,4.8)} \node[pin,anchor=south] at (8.3,4.85){VREF 5\,V};
\gnd{(8.7,2.5)} \node[pin,anchor=east] at (8.6,2.5){VSS};
% controllo SPI
\coordinate (a_clk) at (11.1,3.3); \coordinate (a_dout) at (11.1,2.9); \coordinate (a_cs) at (11.1,2.5); \coordinate (a_vdd) at (11.1,3.7);
\draw[wire] (adc.east|-0,3.7)--(a_vdd); \node[pin,anchor=west] at (a_vdd){8 VDD}; \draw[pwr] (a_vdd)--(11.5,3.7)--(11.5,4.8)--(0.6,4.8);
\draw[wire] (adc.east|-0,3.3)--(a_clk); \node[pin,anchor=west] at (a_clk){7 CLK};
\draw[wire] (adc.east|-0,2.9)--(a_dout); \node[pin,anchor=west] at (a_dout){6 DOUT};
\draw[wire] (adc.east|-0,2.5)--(a_cs); \node[pin,anchor=west] at (a_cs){5 /CS};
% collegamenti verso FPGA (etichette visibili, sfondo bianco)
\draw[wire] (a_clk)--(12.0,3.3)--(12.0,1.3)--(5.7,1.3)--(p_acsk);
\node[lab,fill=white,inner sep=0.7pt] at (8.6,1.35) {CLK $\to$ 74};
\draw[wire] (a_cs)--(12.3,2.5)--(12.3,1.05)--(6.2,1.05)--(p_acs);
\node[lab,fill=white,inner sep=0.7pt] at (8.6,1.1) {/CS $\to$ 73};
% DOUT -> partitore -> miso_p
\draw[wire] (a_dout)--(12.6,2.9); \rh{12.6}{13.6}{2.9}{1\,k} \dotj{(13.6,2.9)}
\draw[wire] (13.6,2.9)--(13.6,1.55)--(6.7,1.55)--(p_amiso); \node[lab,anchor=south] at (10.0,1.6){DOUT$\div$ $\to$ 85};
\rv{13.6}{2.9}{2.1}{2\,k} \gnd{(13.6,2.1)}
\draw[wire] (p_amosi)--(7.2,6.8); \node[lab,anchor=south] at (7.2,6.8){75 n.c.};
\end{tikzpicture}%
}
\caption{}
\label{fig:board}
\end{figure}

% =====================================================================
\section{The FPGA SoC}
\label{sec:soc}
% =====================================================================

The Gowin GW2AR-18 holds the custom SoC. It is a 20k LUT class FPGA, and the
\,AR variant has an 8\,MByte SDR SDRAM die inside the same package, reachable on
dedicated fabric pins (no external SDRAM soldering). Figure~\ref{fig:soc} is the
top level block diagram.

\subsection{Block diagram}

\begin{figure}[tp]
\centering
\resizebox{0.86\textwidth}{!}{%
\begin{tikzpicture}[
    every node/.style={font=\small},
    blk/.style={draw, line width=0.8pt, fill=white, minimum width=3.7cm, minimum height=1.7cm, align=center, text width=3.5cm},
    subblk/.style={draw, line width=0.8pt, fill=white, minimum width=1.6cm, minimum height=0.7cm, align=center, font=\scriptsize, text width=1.5cm},
    preblk/.style={draw, line width=0.8pt, fill=white, minimum width=2.5cm, minimum height=0.6cm, align=center, font=\scriptsize},
    pin/.style={circle, draw, fill=white, line width=0.6pt, inner sep=1pt, minimum size=5pt},
    lbl/.style={font=\scriptsize},
    sig/.style={line width=0.6pt},
    clk/.style={-{Stealth[length=5pt]}, line width=0.7pt, dashed},
    rstl/.style={line width=0.48pt, draw=black!68, densely dashdotted, rounded corners=2pt},
    rstar/.style={-{Stealth[length=5pt]}, line width=0.52pt, draw=black!68, densely dashdotted, rounded corners=2pt},
    rstdot/.style={circle, draw, line width=0.7pt, fill=white, inner sep=0pt, minimum size=4.5pt}
]

% ==============================================================================
% MACRO PER FRECCE CUSTOM
% ==============================================================================
\newcommand{\fatR}[4]{
    \draw[fill=white, line width=0.8pt] 
        (#1, #3+0.18) -- (#2-0.35, #3+0.18) -- (#2-0.35, #3+0.35) -- 
        (#2, #3) -- 
        (#2-0.35, #3-0.35) -- (#2-0.35, #3-0.18) -- (#1, #3-0.18) -- cycle;
    \node[font=\scriptsize\sffamily] at ($(#1, #3)!0.5!(#2, #3)$) {#4};
}

\newcommand{\fatL}[4]{
    \draw[fill=white, line width=0.8pt] 
        (#2, #3+0.18) -- (#1+0.35, #3+0.18) -- (#1+0.35, #3+0.35) -- 
        (#1, #3) -- 
        (#1+0.35, #3-0.35) -- (#1+0.35, #3-0.18) -- (#2, #3-0.18) -- cycle;
    \node[font=\scriptsize\sffamily] at ($(#1, #3)!0.5!(#2, #3)$) {#4};
}

\newcommand{\fatBidir}[4]{
    \draw[fill=white, line width=0.8pt] 
        (#1+0.35, #3+0.18) -- (#2-0.35, #3+0.18) -- (#2-0.35, #3+0.35) -- 
        (#2, #3) -- 
        (#2-0.35, #3-0.35) -- (#2-0.35, #3-0.18) -- (#1+0.35, #3-0.18) -- 
        (#1+0.35, #3-0.35) -- (#1, #3) -- (#1+0.35, #3+0.35) -- cycle;
    \node[font=\scriptsize\sffamily] at ($(#1, #3)!0.5!(#2, #3)$) {#4};
}

\newcommand{\fatDown}[4]{
    \draw[fill=white, line width=0.8pt] 
        (#1-0.18, #2) -- (#1-0.18, #3+0.35) -- (#1-0.35, #3+0.35) -- 
        (#1, #3) -- 
        (#1+0.35, #3+0.35) -- (#1+0.18, #3+0.35) -- (#1+0.18, #2) -- cycle;
    \node[font=\tiny\sffamily, rotate=90] at (#1, {(#2+#3)/2}) {#4};
}

\def\BusL{-0.5} \def\BusR{0.5} 
\def\Ledge{-5.15} \def\Redge{5.15}


% ==============================================================================
% BUS CENTRALE
% ==============================================================================
\draw[fill=white, line width=1.2pt]
    (-0.5, -9.5) -- (-0.5, 14.2) -- (-0.9, 14.2) -- (0, 15.4) --
    (0.9, 14.2) -- (0.5, 14.2) -- (0.5, -9.5) -- (0.9, -9.5) --
    (0, -10.2) -- (-0.9, -9.5) -- cycle;
\node[rotate=90, font=\bfseries\sffamily] at (0, 2.1) {On Chip Bus (Wishbone, 32 bit)};


% ==============================================================================
% BLOCCHI DI SINISTRA
% ==============================================================================
\node[blk] (L1) at (-7.0, 9.2) {\textbf{PicoRV32 CPU}\\bus master M0\\40.5 MHz, RV32IMC};
\fatBidir{\Ledge}{\BusL}{9.2+0.55}{M0 data}
\fatR{\Ledge}{\BusL}{9.2}{M0 control}
\fatR{\Ledge}{\BusL}{9.2-0.55}{M0 address}

% Doppio ruolo = doppio set di frecce: M1 (master, verso il bus) e S5 (slave,
% verso il blocco), come per gli altri slave. Il blocco e' un CONTENITORE: i
% moduli fisici che lo compongono sono disegnati dentro (vedi sotto, dopo gli
% altri blocchi: il nodo fit (L2) e' creato nella sezione "internals").
\fatBidir{\Ledge}{\BusL}{7.05}{M1 data}
\fatR{\Ledge}{\BusL}{6.5}{M1 control}
\fatR{\Ledge}{\BusL}{5.95}{M1 address}
\fatBidir{\Ledge}{\BusL}{5.4}{S5 data}
\fatL{\Ledge}{\BusL}{4.85}{S5 control}
\fatL{\Ledge}{\BusL}{4.3}{S5 address}

% SDRAM port spezzato in 3 blocchi: SIP (protocollo, parla col die e riceve il
% drain), fetch FSM (lettura, avviata dall'IRQ), BSRAM+S0 (cio' che il bus vede).
\node[blk, minimum height=0.9cm] (L3) at (-7.0, 2.0) {\textbf{SDRC\_HS SIP}\\\scriptsize SDRAM controller};
\node[blk, minimum height=0.7cm] (fetchN) at (-7.0, 0.75) {\textbf{fetch FSM} (\texttt{tst\_})};
% Slave S0 = CONTENITORE con dentro i due moduli fisici che il bus vede:
% la BSRAM dello snapshot e il contatore fill_cnt. Le frecce data/control/
% address attaccano al contenitore: e' S0 (tutto insieme) a rispondere al bus.
\node[blk, minimum width=3.2cm, text width=3.05cm, minimum height=0.9cm] (bsramN) at (-6.8, -0.5) {\texttt{fft\_bsram}\\\scriptsize snapshot $512{\times}16$};
\node[subblk, minimum width=1.5cm, text width=1.4cm, minimum height=0.7cm] (fillcnt) at (-9.25, -0.5) {\texttt{fill\_cnt}\\(0x90)};
\node[lbl] (s0lbl) at (-7.6, -1.25) {slave S0 (0x48)};
\node[draw, line width=0.8pt, minimum width=5.0cm, minimum height=1.6cm, inner sep=0pt] (s0box) at (-7.65, -0.65) {};
\draw[{Stealth[length=5pt]}-{Stealth[length=5pt]}, line width=0.8pt] (L3.south) -- (fetchN.north)
  node[midway, anchor=west, lbl, xshift=2pt]{rd strobe / data};
\draw[-{Stealth[length=5pt]}, line width=0.8pt] (fetchN.south -| bsramN.north) -- (bsramN.north);
\draw[-{Stealth[length=5pt]}, line width=0.8pt] (fetchN.west) -| (fillcnt.north);
\fatBidir{\Ledge}{\BusL}{-0.5+0.55}{S0 data}
\fatL{\Ledge}{\BusL}{-0.5}{S0 control}
\fatL{\Ledge}{\BusL}{-0.5-0.55}{S0 address}

% SPI NOR = slave S8: la CPU esegue le sequenze W25Q in firmware (norflash.c).
\node[blk] (L4) at (-7.0, -2.8) {\textbf{SPI master generic}\\slave S8 (0x38)\\NOR flash (W25Q)};
\node[preblk] (preL4) at (-7.0, -4.45) {SPI $\div$64\\633 kHz};
\draw[clk] (L4.south) -- (preL4.north);
\fatBidir{\Ledge}{\BusL}{-2.8+0.55}{S8 data}
\fatL{\Ledge}{\BusL}{-2.8}{S8 control}
\fatL{\Ledge}{\BusL}{-2.8-0.55}{S8 address}

% Display TFT ST7789 = slave S9 (0x5800_0000), SPI mode 3 TX-only, ~6.75 MHz.
% Prescaler fisso /6 separato sotto il blocco, come tutti gli altri divisori.
\node[blk] (disp) at (-7.0, -6.0) {\textbf{SPI display}\\slave S9 (0x58)\\\scriptsize TFT ST7789, mode 3};
\node[preblk] (preDisp) at (-7.0, -7.55) {SPI $\div$6\\6.75 MHz};
\draw[clk] (disp.south) -- (preDisp.north);
\fatBidir{\Ledge}{\BusL}{-6.0+0.55}{S9 data}
\fatL{\Ledge}{\BusL}{-6.0}{S9 control}
\fatL{\Ledge}{\BusL}{-6.0-0.55}{S9 address}


% ==============================================================================
% BLOCCHI DI DESTRA E PRESCALER
% ==============================================================================
\node[blk] (R1) at (7.0, 9.2) {\textbf{UART generic}\\slave S6 (0x2)\\char UART (to ESP32)};
\node[preblk] (preR1) at (7.0, 7.4) {baud $\div$352\\115200};
\draw[clk] (R1.south) -- (preR1.north);
\fatBidir{\BusR}{\Redge}{9.2+0.55}{S6 data}
\fatR{\BusR}{\Redge}{9.2}{S6 control}
\fatR{\BusR}{\Redge}{9.2-0.55}{S6 address}

\node[blk] (R2) at (7.0, 5.2) {\textbf{SPI master (ADC)}\\slave S1 (0x4)\\mic samples};
\node[preblk] (preR2) at (7.0, 3.4) {prescaler $\div$54\\750 kHz};
\draw[clk] (R2.south) -- (preR2.north);
\fatBidir{\BusR}{\Redge}{5.2+0.55}{S1 data}
\fatR{\BusR}{\Redge}{5.2}{S1 control}
\fatR{\BusR}{\Redge}{5.2-0.55}{S1 address}

\node[blk] (R3) at (7.0, 1.2) {\textbf{PWM 10 bit}\\slave S2 (0x5)\\speaker tone};
\node[preblk] (preR3) at (7.0, -0.6) {carrier $\div$256\\158.2 kHz};
\draw[clk] (R3.south) -- (preR3.north);
\fatBidir{\BusR}{\Redge}{1.2+0.55}{S2 data}
\fatR{\BusR}{\Redge}{1.2}{S2 control}
\fatR{\BusR}{\Redge}{1.2-0.55}{S2 address}

\node[blk] (R4) at (7.0, -2.8) {\textbf{PWM 4 bit}\\slave S3 (0x6)\\LED};
\node[preblk] (preR4) at (7.0, -4.6) {carrier $\div$256\\105.5 kHz};
\draw[clk] (R4.south) -- (preR4.north);
\fatBidir{\BusR}{\Redge}{-2.8+0.55}{S3 data}
\fatR{\BusR}{\Redge}{-2.8}{S3 control}
\fatR{\BusR}{\Redge}{-2.8-0.55}{S3 address}

\node[blk] (R5) at (7.0, -6.6) {\textbf{GPIO 1 bit}\\slave S4 (0x7)};
\fatBidir{\BusR}{\Redge}{-6.6+0.55}{S4 data}
\fatR{\BusR}{\Redge}{-6.6}{S4 control}
\fatR{\BusR}{\Redge}{-6.6-0.55}{S4 address}


% ==============================================================================
% COMPONENTI SECONDARI, INTERNI AUDIO E PLL
% ==============================================================================
\node[blk, minimum width=3.0cm, minimum height=1.1cm] (mem) at (-7.0,12.0) {\textbf{ITCM + DTCM}\\block RAM (BSRAM)};
\draw[{Stealth[length=7pt,width=7pt]}-{Stealth[length=7pt,width=7pt]}, line width=1.5pt] (mem.south) -- (L1.north) node[midway, right, lbl]{local bus};

\node[blk, minimum width=3.2cm, minimum height=1.2cm] (pll) at (-7.0,-9.2) {\textbf{rPLL}\\clkout 40.5 MHz\\clkoutp $+1.54$ ns};

% Le reti di clock sono disegnate come ALBERO radicato nel bus, dopo che tutti
% i blocchi e i pin sono stati definiti (sezione "CLOCK TREE" in fondo).

% UART comandi NOR = slave S7: la CPU riceve da qui gli opcode storage
% dell'ESP32 (pin 72/20) e li esegue sullo SPI S8.
\node[blk] (R0) at (7.0, 13.0) {\textbf{UART generic}\\slave S7 (0x28)\\NOR cmds (to ESP32)};
\node[preblk] (preR0) at (7.0, 11.2) {baud $\div$352\\115200};
\draw[clk] (R0.south) -- (preR0.north);
\fatBidir{\BusR}{\Redge}{13.0+0.55}{S7 data}
\fatR{\BusR}{\Redge}{13.0}{S7 control}
\fatR{\BusR}{\Redge}{13.0-0.55}{S7 address}

% ── Audio accelerator: contenitore con i MODULI FISICI veri (dma.vhd) ────────
% dma_proc (FSM cattura, master M1) -> spi_raw_buf (BSRAM ping-pong);
% fft_proc (FSM drain) + FFT core (IP) -> fft_result_buf (BSRAM);
% drain: fft_result_buf -> SIP (porta di scrittura dedicata, NON bus);
% IRQ frame-done: da fft_proc verso la fetch FSM (e irq[20] della CPU).
\node[align=center, font=\small] (acctitle) at (-7.6, 7.45) {\textbf{Audio accelerator}\\master M1 + slave S5 (0x3)};
\node[subblk, minimum width=1.7cm, text width=1.6cm] (dmaP)    at (-9.2, 6.35) {\texttt{dma\_proc}\\FSM (capture)};
\node[subblk, fill=black!7, minimum width=2.6cm, text width=2.5cm] (rawB) at (-6.6, 6.35) {\texttt{spi\_raw\_buf}\\BSRAM $1024{\times}16$};
\node[subblk, minimum width=1.7cm, text width=1.6cm] (fftproc) at (-9.2, 4.95) {\texttt{fft\_proc}\\FSM (drain)};
\node[subblk, minimum width=2.6cm, text width=2.5cm] (fftcore) at (-6.6, 4.95) {FFT 512\\Gowin IP core};
\node[subblk, fill=black!7, minimum width=2.6cm, text width=2.5cm] (resbuf) at (-6.6, 3.55) {\texttt{fft\_result\_buf}\\BSRAM $512{\times}16$};
\draw[-{Stealth[length=5pt]}, line width=0.8pt] (dmaP.east) -- (rawB.west);
\draw[-{Stealth[length=5pt]}, line width=0.8pt] (dmaP.south) -- (fftproc.north);
\node[lbl, anchor=west] at (-9.1, 5.7) {trig $/512$};
\draw[-{Stealth[length=5pt]}, line width=0.8pt] (rawB.south) -- (fftcore.north);
\draw[-{Stealth[length=5pt]}, line width=0.8pt] (fftproc.east) -- (fftcore.west);
\draw[-{Stealth[length=5pt]}, line width=0.8pt] (fftcore.south) -- (resbuf.north);
\node[draw, line width=0.8pt, minimum width=5.05cm, minimum height=4.92cm, inner sep=0pt] (L2) at (-7.675, 5.44) {};

% Drain spettro: fft_result_buf -> SIP via porta di scrittura DEDICATA
% (sdr_own), NON via bus -> la freccia entra nel blocco SIP.
% IRQ frame-done: da fft_proc, avvia la fetch FSM (la stessa linea va anche a
% irq[20] della CPU, che pero' fa polling su fill_cnt).
\draw[-{Stealth}, line width=0.9pt] (resbuf.south) -- (L3.north -| resbuf.south);
\node[lbl, anchor=west] at (-6.4, 2.72) {drain (write port)};
\draw[-{Stealth}, line width=0.7pt, dashed] (fftproc.south) -- (-9.2,1.35) -- (-8.3,1.35) -- (fetchN.north -| -8.3,0);
\node[lbl, fill=white, inner sep=1pt] at (-9.2,3.6) {IRQ frame done};


% ==============================================================================
% SDRAM DIE (Spostato fuori per evitare sovrapposizioni)
% ==============================================================================
\node[blk, minimum width=2.6cm, minimum height=1.3cm] (die) at (-15.0,2.0) {\textbf{SDRAM die}\\ \footnotesize 8 MB, 32-bit};
\draw[{Stealth[length=6pt,width=6pt]}-{Stealth[length=6pt,width=6pt]}, line width=1.2pt] (die.east) -- (L3.west);
% Etichetta dei segnali del die: appesa SOTTO il tratto ESTERNO della
% connessione SIP<->die (fuori dal bounding box del SoC, lontano dall'IRQ).
\node[lbl, font=\tiny, align=center] at (-12.3,1.0) {A[10:0],\\DQ[31:0],\\RAS/CAS/WE/CS,\\CKE, DQM};


% ==============================================================================
% PIN ESTERNI (Anche loro sicuri fuori dal Bounding Box)
% ==============================================================================
\foreach \i/\y/\nm in {1/-2.35/{flash SCK = pin 42}, 2/-2.65/{flash CS = pin 41}, 3/-2.95/{flash MOSI = pin 51}, 4/-3.25/{flash MISO = pin 48}}{
  \node[pin] (fp\i) at (-12.5,\y){}; \node[lbl,anchor=east] at (fp\i.west){\nm};
  \draw[sig] (fp\i) -- (fp\i -| L4.west);
}
% pin del display TFT (dal .cst: 27/28/29/30/31)
\foreach \i/\y/\nm in {1/-5.3/{TFT SCL = pin 27}, 2/-5.55/{TFT SDA = pin 28}, 3/-5.8/{TFT DC = pin 29}, 4/-6.05/{TFT RST = pin 30}, 5/-6.3/{TFT CS = pin 31}}{
  \node[pin] (dp\i) at (-12.5,\y){}; \node[lbl,anchor=east] at (dp\i.west){\nm};
  \draw[sig] (dp\i) -- (dp\i -| disp.west);
}
\node[pin] (xt) at (-12.5,-9.2){}; \node[lbl,anchor=east] at (xt.west){clk\_i = pin 4 (27 MHz)}; \draw[sig](xt)--(pll.west);

\foreach \i/\y/\nm in {1/9.5/{UART char TX = pin 17}, 2/8.9/{UART char RX = pin 18}}{
  \node[pin] (up\i) at (12.2,\y){}; \node[lbl,anchor=west] at (up\i.east){\nm};
  \draw[sig] (up\i -| R1.east) -- (up\i);
}
% La Flash command UART (R0) ha i SUOI pin verso l'ESP32, separati da 17/18
\node[pin] (fux1) at (12.2,13.3){}; \node[lbl,anchor=west] at (fux1.east){flash UART TX = pin 72};
\node[pin] (fux2) at (12.2,12.7){}; \node[lbl,anchor=west] at (fux2.east){flash UART RX = pin 20};
\draw[sig] (R0.east) |- (fux1);
\draw[sig] (R0.east) |- (fux2);
\foreach \i/\y/\nm in {1/6.0/{ADC SCK = pin 74}, 2/5.5/{ADC MOSI = pin 75}, 3/5.0/{ADC MISO = pin 85}, 4/4.5/{ADC CS = pin 73}}{
  \node[pin] (ap\i) at (12.2,\y){}; \node[lbl,anchor=west] at (ap\i.east){\nm};
  \draw[sig] (ap\i -| R2.east) -- (ap\i);
}
\node[pin] (spk) at (12.2,1.2){}; \node[lbl,anchor=west] at (spk.east){Speaker pwm\_10 = pin 76 (MOSFET)}; \draw[sig](R3.east)--(spk);
\node[pin] (led) at (12.2,-2.8){}; \node[lbl,anchor=west] at (led.east){LED pwm\_4 = pin 77 (MOSFET)}; \draw[sig](R4.east)--(led);
\node[pin] (gp)  at (12.2,-6.6){}; \node[lbl,anchor=west] at (gp.east){gpio\_1\_o = pin 80 (=1)}; \draw[sig](R5.east)--(gp);


% ==============================================================================
% CLOCK TREE: il rPLL inietta i clock nel bus, da li' un ramo per blocco.
% Convenzione grafica: il bus fa da dorsale di distribuzione; ogni ramo
% tratteggiato porta scritto il SUO clock. Niente rotaie laterali.
% ==============================================================================
% Iniezioni nella dorsale (in basso): clkout+clkoutp dal rPLL, clk_i dal tap
% PRE-PLL (pallino sul filo del quarzo).
\draw[clk] (pll.east) -- (\BusL,-9.2);
\node[lbl, fill=white, inner sep=1pt, align=center] at (-2.9,-9.2) {clkout 40.5 MHz\\clkoutp $+1.54$ ns};
\fill (-10.5,-9.2) circle (1.6pt);
\draw[clk] (-10.5,-9.2) -- (-10.5,-8.2) -- (\BusL,-8.2);
\node[lbl, fill=white, inner sep=1pt] at (-2.9,-8.2) {clk\_i 27 MHz};
% Rami colonna SINISTRA (clkout 40.5 MHz).
\draw[clk] (\BusL,12.0) -- (mem.east);
\node[lbl, fill=white, inner sep=1pt] at (-2.9,12.0) {clkout 40.5 MHz};
\draw[clk] (\BusL,10.5) -| ([xshift=6mm]L1.north);
\node[lbl, fill=white, inner sep=1pt] at (-2.9,10.5) {clkout 40.5 MHz};
\draw[clk] (\BusL,3.3) -- (L2.east |- 0,3.3);
\node[lbl, fill=white, inner sep=1pt] at (-2.9,3.3) {clkout 40.5 MHz};
\draw[clk] (\BusL,2.0) -- (L3.east);
\node[lbl, fill=white, inner sep=1pt] at (-2.9,2.0) {clkout 40.5 MHz};
\draw[clk] (\BusL,0.75) -- (fetchN.east);
\node[lbl, fill=white, inner sep=1pt] at (-2.9,0.75) {clkout 40.5 MHz};
\draw[clk] (\BusL,-1.7) -| ([xshift=19mm]s0box.south);
\node[lbl, fill=white, inner sep=1pt] at (-2.9,-1.7) {clkout 40.5 MHz};
\draw[clk] (\BusL,-3.9) -| ([xshift=6mm]L4.south);
\node[lbl, fill=white, inner sep=1pt] at (-2.9,-3.9) {clkout 40.5 MHz};
\draw[clk] (\BusL,-4.95) -| ([xshift=6mm]disp.north);
\node[lbl, fill=white, inner sep=1pt] at (-2.9,-4.95) {clkout 40.5 MHz};
% Ramo per il die SDRAM (clkoutp): esce dalla dorsale in alto e scende al die.
\draw[clk] (\BusL,13.4) -| (die.north);
\node[lbl, fill=white, inner sep=1pt] at (-5.6,13.4) {clkoutp $+1.54$ ns};
% Rami colonna DESTRA: clkout per S7/S6/S1/S2, clk_i 27 MHz per LED e GPIO.
\draw[clk] (\BusR,11.8) -| ([xshift=-6mm]R0.south);
\node[lbl, fill=white, inner sep=1pt] at (3.0,11.8) {clkout 40.5 MHz};
\draw[clk] (\BusR,8.05) -| ([xshift=-6mm]R1.south);
\node[lbl, fill=white, inner sep=1pt] at (3.0,8.05) {clkout 40.5 MHz};
\draw[clk] (\BusR,4.0) -| ([xshift=-6mm]R2.south);
\node[lbl, fill=white, inner sep=1pt] at (3.0,4.0) {clkout 40.5 MHz};
\draw[clk] (\BusR,0.0) -| ([xshift=-6mm]R3.south);
\node[lbl, fill=white, inner sep=1pt] at (3.0,0.0) {clkout 40.5 MHz};
\draw[clk] (\BusR,-3.95) -| ([xshift=-6mm]R4.south);
\node[lbl, fill=white, inner sep=1pt] at (3.0,-3.95) {clk\_i 27 MHz};
\draw[clk] (\BusR,-5.45) -| ([xshift=-6mm]R5.north);
\node[lbl, fill=white, inner sep=1pt] at (3.0,-5.45) {clk\_i 27 MHz};


% ==============================================================================
% RESET TREE: un solo reset di sistema (pll_lock), iniettato sotto il bus come
% i clock, e distribuito ai blocchi. Convenzione: PALLINO = ingresso attivo
% basso (resetn / rst_i='0'), FRECCIA piena = ingresso attivo alto. Quasi tutto
% il SoC e' attivo basso (esce dal reset quando il PLL aggancia); solo le due
% PWM usano la versione invertita (not pll_lock), quindi attive alte.
% ==============================================================================
% Iniezione sotto il bus dal lock del rPLL.
\draw[rstl] (pll.north) -- (\BusL,-8.6);
\node[lbl, fill=white, inner sep=1pt] at (-4.3,-8.6) {reset $=$ \texttt{pll\_lock}};
% Ogni ramo del reset porta scritto "reset" (etichetta in spazio libero, sfondo
% bianco) e finisce col PALLINO (attivo basso) o con la FRECCIA piena (attivo alto).
% Le quote y dei rami sono scelte nei vuoti fra blocchi, lontano dalle frecce-bus.
% Rami colonna SINISTRA (pallino = ingresso attivo basso).
\draw[rstl] (\BusL,10.0) -| ([xshift=-9mm]L1.north);
\node[rstdot] at ([xshift=-9mm]L1.north) {};
\node[lbl, fill=white, inner sep=1pt] at (-4.5,10.0) {reset};
% (il reset del controller SDRAM, I_sdrc_rst_n, e' citato in didascalia: il
%  cluster SIP/fetch/S0 ha tutti e quattro i lati occupati, niente ramo qui.)
\draw[rstl] (\BusL,-1.85) -| ([xshift=-9mm]L4.north);
\node[rstdot] at ([xshift=-9mm]L4.north) {};
\node[lbl, fill=white, inner sep=1pt] at (-5.0,-1.85) {reset};
\draw[rstl] (\BusL,-7.1) -| ([xshift=-9mm]disp.south);
\node[rstdot] at ([xshift=-9mm]disp.south) {};
\node[lbl, fill=white, inner sep=1pt] at (-4.5,-7.1) {reset};
% Rami colonna DESTRA.
\draw[rstl] (\BusR,10.3) -| ([xshift=9mm]R1.north);
\node[rstdot] at ([xshift=9mm]R1.north) {};
\node[lbl, fill=white, inner sep=1pt] at (4.5,10.3) {reset};
\draw[rstl] (\BusR,-7.55) -| ([xshift=9mm]R5.south);
\node[rstdot] at ([xshift=9mm]R5.south) {};
\node[lbl, fill=white, inner sep=1pt] at (4.5,-7.55) {reset};
% Le due PWM: attive ALTE (freccia piena, niente pallino).
\draw[rstar] (\BusR,2.4) -| ([xshift=9mm]R3.north);
\node[lbl, fill=white, inner sep=1pt] at (4.5,2.4) {reset};
\draw[rstar] (\BusR,-1.6) -| ([xshift=9mm]R4.north);
\node[lbl, fill=white, inner sep=1pt] at (4.5,-1.6) {reset};


% ==============================================================================
% BOUNDING BOX DEL SOC (Ora sagomato sui blocchi interni)
% ==============================================================================
\begin{scope}[on background layer]
% Il rettangolo usa i nodi più esterni INTERNI per fittare, escludendo die e pin
\node[draw, line width=1.2pt, fit=(mem)(L2)(L4)(preL4)(disp)(pll)(R0)(preR0)(R1)(preR1)(R5)(preR4), inner sep=20pt, rounded corners=3pt] (socbox){};
\end{scope}
\node[anchor=south east, font=\bfseries\large] at (socbox.south east) {SoC\ \ };

\end{tikzpicture}%
}
\caption{SoC top level (black and white). Each block reaches the central 32 bit Wishbone bus with three thick arrows: \emph{data} is bidirectional, while \emph{control} and \emph{address} arrows point toward the bus for the two masters (PicoRV32, audio accelerator) and toward the block for the slaves, indicating signal direction. The audio accelerator is both a master (M1, to read the ADC) and a slave (S5, for its control registers), so it reaches the bus with \emph{two} sets of arrows: the M1 set drawn in the master direction and the S5 set drawn in the slave direction, like every other slave. Inside it the real modules of \texttt{dma.vhd} are drawn: \texttt{dma\_proc} (the M1 master) filling the sample BSRAM, \texttt{fft\_proc} feeding the FFT core and collecting into the result BSRAM. Its spectrum \emph{drain} does not cross the bus at all, it owns a dedicated write port of the SDRAM controller, which is why the solid drain arrow enters the \emph{SIP} block directly. The SDRAM port is drawn as its real pieces: the SDRC\_HS SIP (the controller that speaks to the die and owns the write port), the \emph{fetch FSM} (\texttt{tst\_} in the RTL), and the two things the bus actually sees as slave S0, the BSRAM snapshot and the \texttt{fill\_cnt} frame counter. The dashed arrow is the frame done IRQ: in hardware it starts the fetch FSM, which reads the new spectrum back through the SIP, fills the BSRAM and finally bumps \texttt{fill\_cnt}; the same line also reaches the PicoRV32 as \texttt{irq[20]}, but the firmware ignores it and polls \texttt{fill\_cnt} over the bus instead (Section~\ref{sec:sdram}). The thin dashed nets are the clocks, drawn as a distribution tree rooted in the bus column to keep the routing readable: at the bottom the rPLL injects \texttt{clkout} (40.5\,MHz) and \texttt{clkoutp} ($+1.54$\,ns) into the spine, and the raw 27\,MHz crystal, tapped before the PLL (the dot on the \texttt{clk\_i} wire), enters next to it; each block then receives its own labelled branch, \texttt{clkout} for all the SoC logic, \texttt{clk\_i} (27\,MHz) for the LED PWM and the GPIO, and \texttt{clkoutp} only for the SDRAM die, leaving the SoC at the top left. The dash dotted net labelled \emph{reset} is the single system reset, the rPLL \texttt{lock} (\texttt{pll\_lock}) injected under the bus like the clocks, so the SoC leaves reset only when the PLL locks; a branch ends in a \emph{circle} where the reset input is active low (as on the CPU \texttt{resetn}, the SDRAM controller \texttt{rst\_n}, and \texttt{rst\_i} tested against \texttt{'0'} on every UART, SPI and the GPIO) and in a \emph{filled arrow} where it is active high (the two PWMs, which take \texttt{not pll\_lock}). To keep the figure readable the net is drawn to one representative block of each kind; the SDRAM controller shares it too, while the ADC SPI (tied to ``never reset'') and the accelerator (no synchronous reset) stay unmarked. ITCM and DTCM are CPU local block RAM, not on the Wishbone. The character UART generic slave S6 (\texttt{0x2}) reaches the ESP32 on pins 17 and 18 and carries the protocol characters plus the \texttt{\$} diagnostic lines; the pin is driven by this UART alone (the legacy boot banner and hardware decoder drivers are detached). The \textbf{NOR flash} path now runs through the CPU: the storage commands from the ESP32 land on the S7 UART (pins 72/20, top right), the firmware executes the W25Q sequences on the S8 generic SPI engine (pins 42, 41, 51, 48, left), and the old \texttt{flash\_ctrl} hardware block is retired (Section~\ref{sec:flashctrl}). The \textbf{SPI display} slave S9 (\texttt{spi\_display.vhd}) drives the ST7789 TFT on five spare pins (27 to 31: SCL, SDA, DC, RST, CS), through its fixed $\div6$ prescaler (6.75\,MHz) drawn below the block like every other peripheral divider (Section~\ref{sec:display}). Every external connection carries its real FPGA pin number from the constraints file.}
\label{fig:soc}
\end{figure}


\subsection{The on chip bus and the address map}

The interconnect (\texttt{wb\_interconnect.vhd}) is a shared bus crossbar with
\textbf{two masters and ten slaves}, all live: the two ports that earlier
revisions kept reserved for the flash migration (S7, S8) now carry it
(Section~\ref{sec:flashctrl}), and a tenth port (S9) was added for the TFT
display (Section~\ref{sec:display}). The two masters are the PicoRV32 CPU
(port M0) and the audio accelerator (port M1). When both request the bus in the
same cycle, M1 wins: the accelerator is sample rate bound and must not be
stalled, while the CPU can wait a cycle. The slave that answers a transaction is
chosen by decoding the top five address bits \texttt{addr[31:27]}.

Five bits, not four, because of where the soft core keeps its own memory. The IP
treats every address with \texttt{addr[31:29]=000} (\texttt{0x0}--\texttt{0x1FFF\_FFFF})
as \emph{internal} (its ITCM, DTCM and native UART): an external read there never
completes, it just stalls the core. The SDRAM slave therefore lives at
\texttt{0x4800\_0000} (\texttt{addr[31:29]=010}) and not at \texttt{0x1000\_0000};
since that base falls on a half nibble, with bit 27 set, the decoder needs the
fifth bit to tell it apart from the ADC SPI at \texttt{0x4000\_0000}. The NOR
flash reaches the bus through two slaves with the CPU in between: S7 is the UART
that receives the storage commands from the ESP32 (pins 72 and 20), S8 is the
SPI engine that drives the W25Q (pins 42, 41, 51, 48); the firmware turns one
into the other (Section~\ref{sec:flashctrl}). Table~\ref{tab:addrmap} is the
real map from the RTL.

\begin{table}[H]
\centering
\caption{Address map, decoded from \texttt{addr[31:27]} (five bits). All ten slaves
are live; the NOR flash is reached through two of them (S7 carries the storage
commands from the ESP32, S8 is the SPI engine that drives the chip), with the
firmware in between. SDRAM sits at \texttt{0x4800\_0000}, not in the
\texttt{0x0}--\texttt{0x1} range, because that range is the soft core's own
internal space (see text).}
\label{tab:addrmap}
\begin{tabular}{llll}
\toprule
\textbf{Base} & \texttt{addr[31:27]} & \textbf{Slave} & \textbf{Block} \\
\midrule
\texttt{0x4800\_0000} & \texttt{01001} & S0 & SDRAM fetch FSM + BSRAM snapshot \\
\texttt{0x2000\_0000} & \texttt{00100} & S6 & UART generic (characters, pins 17/18) \\
\texttt{0x2800\_0000} & \texttt{00101} & S7 & UART generic (NOR commands, pins 72/20) \\
\texttt{0x3000\_0000} & \texttt{00110} & S5 & Audio accelerator control registers \\
\texttt{0x3800\_0000} & \texttt{00111} & S8 & SPI master generic (NOR chip W25Q) \\
\texttt{0x4000\_0000} & \texttt{01000} & S1 & SPI master (reads the ADC) \\
\texttt{0x5000\_0000} & \texttt{01010} & S2 & PWM 10 bit (speaker tone) \\
\texttt{0x5800\_0000} & \texttt{01011} & S9 & SPI display (TFT ST7789) \\
\texttt{0x6000\_0000} & \texttt{01100} & S3 & PWM 4 bit (LED) \\
\texttt{0x7000\_0000} & \texttt{01110} & S4 & GPIO 1 bit \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\begin{tikzpicture}[every node/.style={font=\footnotesize}]
\draw[line width=0.8pt] (0,0) rectangle (2.8,1.0);
\draw[line width=0.8pt] (2.8,0) rectangle (12,1.0);
\node at (1.4,0.5) {\textbf{device select}};
\node at (7.4,0.5) {register / offset inside the device};
\node[anchor=south] at (1.4,1.05) {bits [31:27]};
\node[anchor=south] at (7.4,1.05) {bits [26:0]};
\node[anchor=north, font=\scriptsize] at (1.4,-0.05) {picks one of 10 slaves};
\node[anchor=north, font=\scriptsize] at (7.4,-0.05) {128\,MB window, only low bits used};
\end{tikzpicture}
\caption{The 32 bit address is split in two. The top five bits \texttt{addr[31:27]}
pick the slave (Table~\ref{tab:addrmap}); the low 27 bits are the offset inside
that slave, of which each peripheral decodes only the few it needs (the per device
register maps below).}
\label{fig:addrfield}
\end{figure}

A transaction works the standard Wishbone way. The active master drives
\texttt{adr}, \texttt{dat}, \texttt{we}, \texttt{sel} and asserts \texttt{cyc} and
\texttt{stb}. The interconnect broadcasts the address and write data to all slaves
but only routes \texttt{stb} and \texttt{cyc} to the one slave selected by
\texttt{addr[31:27]}; the read data and the \texttt{ack} are multiplexed back from
that same slave. A peripheral therefore needs to do nothing more than raise
\texttt{ack} for one cycle when it is addressed. The decode is purely combinational
on five bits, and any address that matches none of the ten windows falls to a sink
that returns zero with no ack.

\subsection{Arbitration}

There are two masters on the bus, so it needs an arbiter. I used static priority,
not round robin: M1 (the audio accelerator) always wins over M0 (the CPU). In the
RTL this is one line, the select signal \texttt{sel\_m1 = m1\_cyc AND m1\_stb}; when
it is high the mux puts M1's \texttt{adr/dat/we/sel/cyc/stb} on the bus, otherwise
M0's. The \texttt{ack} is steered the same way: \texttt{m0\_ack = slv\_ack AND NOT
sel\_m1} and \texttt{m1\_ack = slv\_ack AND sel\_m1}. So while M1 holds the bus, M0
simply receives no \texttt{ack} and stalls on its current access until M1 releases,
which is the standard Wishbone way to make a master wait, with no extra logic.

The reason for the fixed M1 over M0 priority is timing. The accelerator is sample
rate bound: it must collect every ADC conversion the moment it is ready, and it
cannot be made to wait. The CPU has no hard deadline, so it is the one that
yields. The cost to the CPU is small in practice, because M1's bus traffic is
tiny: the accelerator only uses the bus to service the ADC, one read of the
sample register plus one write to clear the ready flag per conversion, a handful
of cycles out of the 864 that each 21.3\,$\mu$s conversion lasts
(Section~\ref{sec:accel}), so the CPU is blocked for well under 1\,\% of the time
and never for long enough to matter. The heavy transfer, draining the 512
spectrum words to SDRAM, does not cross this bus at all: the accelerator owns a
dedicated write port on the SDRAM controller (Section~\ref{sec:sdram}), so the
drain and the CPU never compete.

\subsection{Three rules the OPEN WB taught the slaves}
\label{sec:wbrules}

The PicoRV32's external bus (Gowin calls it OPEN WB) is undocumented at the
cycle level, and the slaves in this design went through three rounds of bench
debugging before converging on a house style. The three rules below are now
applied to every slave, and the newest peripherals (the NOR SPI engine and the
display SPI) were born with them already in place.

\textbf{Hold the ack while the strobe is up.} My slaves originally pulsed
\texttt{ack} for exactly one cycle, which is legal Wishbone. The OPEN WB master
misses that pulse often enough that a \emph{read} hangs forever (writes are
posted, so they never noticed). The fix is to keep \texttt{ack} asserted for as
long as \texttt{cyc \& stb} is, while still edge gating the \emph{side effects}
of a write on the strobe's rising edge so a held strobe does not re-trigger
them. The GPIO and the two PWMs were retrofitted this way; before the fix,
reading them froze the CPU.

\textbf{Never clear on read.} A register that self-clears when read assumes the
read that cleared it was also delivered intact, and on this bus that is not a
safe assumption. It bit twice: the character UART's \texttt{rx\_valid}
(Section~\ref{sec:fetch} tells the same story for \texttt{fill\_cnt}), and the
SPI engine's \texttt{done} flag, which a dirty STATUS read would consume,
leaving the firmware polling for a completion that had already happened. Both
are now non destructive: \texttt{done} stays set until the \emph{next} TXDATA
write, and status registers can be read any number of times.

\textbf{Put a FIFO behind anything that streams.} The character UART held its
last received byte in a single register; a multi byte burst (the 16 byte NOR
append payload is the worst case) overwrote it while the CPU was busy
elsewhere. The receive side now has a 16 entry FIFO; the read of the data
register pops one byte, with the popped value captured into a holding register
on the first cycle of the access, because the master samples the data a cycle
after the pointer has already moved.

\subsection{The simple slaves: UART and GPIO register maps}

The two slaves without a section of their own are the generic UART (S6) and the GPIO
(S4). The UART is the widest register map in the design; the GPIO is a single bit.

\begin{table}[H]
\centering
\caption{UART generic registers (base \texttt{0x2000\_0000}) and GPIO (base \texttt{0x7000\_0000}).}
\begin{tabular}{llll}
\toprule
\textbf{Device} & \textbf{Offset} & \textbf{Dir} & \textbf{Function} \\
\midrule
UART & \texttt{0x00} & W & transmit a byte (\texttt{dat[7:0]}) \\
UART & \texttt{0x00} & R & \texttt{bit[8]}=rx\_valid, \texttt{bit[7:0]}=received byte (read clears valid) \\
UART & \texttt{0x04} & W & enable \\
UART & \texttt{0x08} & W & disable \\
UART & \texttt{0x0C} & W & \texttt{baud\_div} $=$ clk / baud \\
UART & \texttt{0x10} & W & format: \texttt{dat[1:0]}=parity, \texttt{dat[2]}=stop bits, \texttt{dat[6:3]}=data bits$-5$ \\
UART & \texttt{0x14} & R & \texttt{bit[1]}=rx\_valid, \texttt{bit[0]}=tx\_busy \\
GPIO & \texttt{0x00} & W & \texttt{dat[0]} drives the output pin \\
\bottomrule
\end{tabular}
\end{table}

% =====================================================================
\section{Clock tree}
\label{sec:clocks}
% =====================================================================

A single 27\,MHz clock enters the FPGA on pin 4 (\texttt{clk\_i}). The active rPLL
(\texttt{gowin\_rpll.vhd}) is configured \texttt{IDIV\_SEL}$=1$, \texttt{FBDIV\_SEL}$=2$,
so it divides by 2 and multiplies by 3:
\[
f_\mathrm{clkout} = 27\,\mathrm{MHz}\times \frac{3}{2} = 40.5\,\mathrm{MHz}.
\]
The second output \texttt{clkoutp} is the same 40.5\,MHz but phase shifted; with
\texttt{PSDA\_SEL}$=1$ and \texttt{ODIV\_SEL}$=16$ the shift is one of 16 steps,
$360^{\circ}/16 = 22.5^{\circ}$, which at 40.5\,MHz (period 24.7\,ns) is
$24.7\,\mathrm{ns}\times 22.5/360 = 1.54\,\mathrm{ns}$.

There are three clocks, and which block runs on which matters. The 27\,MHz
(\texttt{clk\_i}) is the PLL reference and also directly clocks the PWM LED and
the GPIO. The 40.5\,MHz (\texttt{clkout}) is the main clock: CPU, Wishbone
interconnect, SPI for the ADC, audio accelerator, SDRAM controller, flash
controller and the UARTs. The speaker PWM also runs on the 40.5\,MHz clock,
deliberately: its registers are written by the CPU over the bus, and keeping
peripheral and bus in the same domain removes the clock domain crossing on the
Wishbone handshake (the LED PWM stays on 27\,MHz, since nothing timing critical
crosses into it). The 40.5\,MHz phase shifted (\texttt{clkoutp}) clocks only the
SDRAM die, to centre its sampling window (Section~\ref{sec:sdram}).
Table~\ref{tab:dividers} gives the base clock and the divider each block uses to
reach its I/O rate.

\begin{table}[H]
\centering
\caption{Base clock and internal divider per block.}
\label{tab:dividers}
\resizebox{\textwidth}{!}{%
\begin{tabular}{llll}
\toprule
\textbf{Block} & \textbf{Base clk} & \textbf{Divider} & \textbf{Result} \\
\midrule
SPI for NOR flash & 40.5 MHz & $\div 64$ & 633 kHz SCK, 1 bit $=1.58\,\mu$s (slow on purpose, Sec.~\ref{sec:si}) \\
SPI for ADC       & 40.5 MHz & $\div 54$ & 750 kHz SCK; 16 SCK $=864$ cyc/sample $\to$ 46.875 kHz \\
SPI for display   & 40.5 MHz & $\div 6$ & 6.75 MHz SCK, mode 3 (TFT ST7789) \\
UART              & 40.5 MHz & $\div 352$ & 115{,}057 baud (115200 within 0.1\,\%) \\
Audio accelerator & 40.5 MHz & paced by ADC & one sample per conversion, 46.875 kHz \\
SDRAM controller  & 40.5 MHz & none & die clocked by \texttt{clkoutp} ($+1.54$ ns) \\
PWM speaker       & 40.5 MHz & DDS phase acc & two tone sine, carrier $40.5\mathrm{M}/256=158.2$ kHz \\
PWM LED           & 27 MHz & DDS phase acc & carrier $27\mathrm{M}/256=105.5$ kHz \\
GPIO              & 27 MHz & none & static output \\
\bottomrule
\end{tabular}%
}
\end{table}

\begin{figure}[H]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
    every node/.style={font=\footnotesize},
    blk/.style={draw, line width=0.6pt, fill=white, rounded corners=2pt, minimum width=3.2cm, minimum height=0.8cm, align=center},
    pll/.style={draw, line width=0.7pt, fill=black!6, rounded corners=2pt, minimum width=3.0cm, minimum height=1.2cm, align=center},
    a/.style={-{Stealth}, line width=0.8pt}
]
\node[blk] (xtal) at (0,0) {clk\_i 27 MHz (pin 4)};
\node[pll] (pll) at (3.8,0) {rPLL\\ \footnotesize $\div2$, $\times3$};
\draw[a] (xtal) -- (pll);
\node[blk] (clk40) at (8.2,1.0) {\texttt{clkout} 40.5 MHz};
\node[blk] (clk40p) at (8.2,-0.6) {\texttt{clkoutp} 40.5 MHz $+1.54$ ns};
\draw[a] (pll.east) -- (clk40.west);
\draw[a] (pll.east) -- (clk40p.west);
\node[blk, minimum width=4.2cm] (main) at (13.6,1.4) {CPU, bus, SPI ADC, accel,};
\node[blk, minimum width=4.2cm] (main2) at (13.6,0.6) {SDRAM ctrl, flash, UART, PWM speaker};
\node[blk, minimum width=4.2cm] (sd) at (13.6,-0.6) {SDRAM die (sampling)};
\node[blk, minimum width=4.2cm] (p27) at (13.6,-2.0) {PWM LED, GPIO};
\draw[a] (clk40.east) -- (main.west);
\draw[a] (clk40.east) |- (main2.west);
\draw[a] (clk40p.east) -- (sd.west);
\draw[a] (xtal.south) |- (p27.west);
\end{tikzpicture}%
}
\caption{Clock tree. The 27\,MHz input is the PLL reference and also clocks the LED
PWM and the GPIO directly. The rPLL produces 40.5\,MHz (\texttt{clkout}) for the
CPU, bus and most peripherals, including the speaker PWM (same domain as the bus,
so no clock domain crossing on its registers), and a $+1.54$\,ns phase shifted copy
(\texttt{clkoutp}) that clocks only the SDRAM die.}
\label{fig:clocktree}
\end{figure}

% =====================================================================
\section{SDRAM controller}
\label{sec:sdram}
% =====================================================================

The 8\,MByte die is internal to the GW2AR-18 package and speaks JEDEC SDR SDRAM. It
is not driven directly: the Gowin SDRC\_HS controller IP sits in front of it and
turns read/write strobes plus a 21 bit \{bank,\,row,\,column\} address into the
ACTIVATE / READ / WRITE / PRECHARGE command sequences, and runs the periodic
refresh on its own. The IP has a single command port, so in front of \emph{it}
there is an ownership mux, which is why the SIP block receives two inputs in
Figure~\ref{fig:soc}: while the accelerator asserts \texttt{sdr\_own} (its drain
phase) the write strobe, address and data come from the accelerator; the rest of
the time the port belongs to the \emph{fetch FSM}, the read side that serves the
CPU (next subsection). The two owners can never collide, because the drain ends,
and only then raises the IRQ that starts the fetch. The controller side uses
these die pins: \texttt{O\_sdram\_dq[31:0]} (32 bit data),
\texttt{O\_sdram\_addr[10:0]}, \texttt{O\_sdram\_ba[1:0]} (4 banks),
\texttt{O\_sdram\_dqm[3:0]}, plus CKE / CS / RAS / CAS / WE and the clock. The whole
path runs on \texttt{clk\_sdram} $=40.5$\,MHz; the die is clocked by
\texttt{clkoutp}, the $+1.54$\,ns phase shifted copy.

Both sides move data in \textbf{bursts of 26 words}, one burst per SDRAM row: the
IP is programmed with \texttt{data\_len} $= 26$, CAS latency 3, sequential
addressing. A 512 word spectrum therefore travels as $20 \times 26 = 520$ word
slots ($\geq 512$; the last eight repeat the final bin and are ignored), each
burst going to its own row (rows 2 to 21 of bank 2, all at column 5), written by
the accelerator after each FFT and read back row by row by the fetch FSM. Two
quirks of this arrangement were found on the bench and are simply worked around
rather than fought. First, the very first transaction after a quiet period was
unreliable, so each drain starts with a \emph{warm up} burst into row 1 whose
content is never read back; the twenty real bursts follow. Second, the IP
delivers one extra \texttt{rd\_valid} pulse per read burst (a 27th word, garbage),
which the capture logic discards by gating on index $< 26$.

I had started, in an earlier version, from single word accesses (burst length 1,
the simplest thing that works) and also looked at the opposite extreme, the full
page burst, which on paper gives the highest throughput. The 26 word row burst is
the middle ground that fits this traffic: the spectrum is contiguous, so a burst
amortises the ACTIVATE / PRECHARGE overhead over 26 words, but it is short enough
that one burst maps onto one row and the addressing stays trivial (row $=$ burst
index, column fixed). One row per burst also means a row is opened exactly once
per frame, never reused, so there is nothing to gain from keeping pages open.

\subsection{Bring up sequence}

The order below was found by testing, not by copying a figure. Each step is the
problem I saw and the parameter that fixed it:

\begin{enumerate}
\item READ issued right after power up $\to$ garbage on every access. The die is not
      ready yet. Fix: assert \texttt{CKE}, wait $\ge 200\,\mu$s for the internal
      regulator.
\item write then immediate read back $\to$ still wrong on the first few accesses,
      then correct. The refresh counter was not locked. Fix: two AUTO REFRESH
      commands before programming the Mode Register.
\item PRECHARGE ALL before the refreshes $\to$ needed to put all four banks in the
      idle state first.
\end{enumerate}

So the bring up is: \texttt{CKE} high and wait $\ge 200\,\mu$s; PRECHARGE ALL; two
AUTO REFRESH; program the Mode Register (CAS latency 3, sequential addressing);
after that, ACTIVATE / READ / WRITE / PRECHARGE are allowed.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  every node/.style={font=\scriptsize},
  sig/.style={line width=0.9pt},
  hi/.style={yshift=12pt}, lo/.style={yshift=0pt},
]
\def\T{0.95} % clock period in cm
\def\rows{0}
% time grid
\foreach \i in {0,...,9} {
  \draw[gray!40, line width=0.3pt] ({\i*\T},0.4) -- ({\i*\T},-5.2);
  \node[gray, anchor=south, font=\tiny] at ({\i*\T},0.45) {T\i};
}
% CLK row
\node[anchor=east] at (-0.25,0.0) {CLK};
\foreach \i in {0,...,8} {
  \draw[sig] ({\i*\T},0) -- ({\i*\T},0.3) -- ({\i*\T+0.5*\T},0.3) -- ({\i*\T+0.5*\T},0) -- ({\i*\T+\T},0);
}
% CMD row (y=-0.9)
\node[anchor=east] at (-0.25,-0.9) {CMD};
\foreach \x/\c in {0/{ACT}, 3/{READ}, 8/{PRE}} {
  \draw[fill=busblue!12, draw=busblue] ({\x*\T-0.30},-1.15) rectangle ({\x*\T+0.30},-0.65);
  \node[busblue] at ({\x*\T},-0.9) {\c};
}
\foreach \x in {1,2,4,5,6,7} { \node[gray] at ({\x*\T},-0.9) {NOP}; }
% BA/ADDR row (y=-1.9)
\node[anchor=east] at (-0.25,-1.9) {A / BA};
\node[fill=accentgreen!15] at ({0*\T},-1.9) {row};
\node[fill=accentgreen!15] at ({3*\T},-1.9) {col};
% DQ row (y=-2.9): valid at T6 (CL=3 after READ at T3)
\node[anchor=east] at (-0.25,-2.9) {DQ};
\draw[sig, gray] (-0.0,-2.9) -- ({6*\T-0.2},-2.9);
\draw[fill=accentred!15, draw=accentred] ({6*\T-0.2},-3.15) rectangle ({6*\T+0.7},-2.65);
\node[accentred] at ({6*\T+0.25},-2.9) {Dout};
\draw[sig, gray] ({6*\T+0.7},-2.9) -- ({9*\T},-2.9);
% timing brackets
\draw[<->] ({0*\T},-3.8) -- ({3*\T},-3.8); \node[anchor=north] at ({1.5*\T},-3.85) {$t_\mathrm{RCD}$ = 3 clk};
\draw[<->] ({3*\T},-4.4) -- ({6*\T},-4.4); \node[anchor=north] at ({4.5*\T},-4.45) {CL = 3 clk};
\draw[<->] ({8*\T},-3.8) -- ({9*\T},-3.8); \node[anchor=north] at ({8.5*\T},-3.85) {$t_\mathrm{RP}$};
\end{tikzpicture}
\caption{Start of an SDRAM read burst at 40.5\,MHz (one clock $=24.7$\,ns). ACTIVATE
at T0 opens the row, $t_\mathrm{RCD}$ later the READ at T3 selects the column, and
with CAS latency 3 the first word is valid on DQ at T6 ($3\times24.7=74$\,ns after
the READ). From there the burst continues, one word per clock, until the programmed
\texttt{data\_len} of 26 words is out; then the controller precharges and the row is
closed, after which $t_\mathrm{RP}$ must elapse before the next ACTIVATE (in the
figure the burst is cut short to fit the page).}
\label{fig:sdram-timing}
\end{figure}

Two more numbers came from the bench. First, skew: at 40.5\,MHz the clock period is
24.7\,ns, and I was reading \texttt{0xFFFE} for \texttt{0xFFFF} on a small fraction of
accesses, because the latching edge landed at the edge of the data valid window
instead of its centre. Driving the die from \texttt{clkoutp}, the phase shifted copy,
fixed it: with \texttt{PSDA\_SEL}$=1$ and \texttt{ODIV\_SEL}$=16$ the shift is
$360^{\circ}/16 = 22.5^{\circ} = 1.54\,\mathrm{ns}$, enough to move the sampling edge
into the centre of the window, and the bit errors disappeared. Second, refresh: all
8192 rows must be refreshed every 64\,ms, so one AUTO REFRESH every
$64\,\mathrm{ms}/8192 = 7.81\,\mu$s, which is $7.81\,\mu\mathrm{s}\times 40.5\,\mathrm{MHz}
= 316$ cycles. My first controller refreshed too rarely and rows lost charge after a
few seconds; the $7.81\,\mu$s counter made retention solid.

\subsection{From the SDRAM to the CPU: the fetch FSM and the BSRAM snapshot}
\label{sec:fetch}

The read side of the SDRAM port is a small state machine that I call the
\emph{fetch FSM} (in the RTL its signals still carry the prefix \texttt{tst\_}: it
was born as the SDRAM bring up test machine, then got promoted to production duty
and the name stuck; renaming it is on the cleanup list). Its job is to move each
freshly written spectrum from the SDRAM to a place the CPU can read calmly.

\begin{figure}[H]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
    every node/.style={font=\footnotesize},
    blk/.style={draw, line width=0.7pt, fill=white, minimum width=2.4cm, minimum height=1.0cm, align=center},
    buf/.style={draw, line width=0.7pt, fill=black!7, minimum width=2.4cm, minimum height=1.0cm, align=center},
    a/.style={-{Stealth}, line width=0.9pt},
    d/.style={-{Stealth}, line width=0.7pt, dashed},
    al/.style={font=\scriptsize, align=center}
]
\node[blk] (sdram) {SDRAM\\ \footnotesize spectrum, rows 2--21};
\node[blk, right=1.9cm of sdram] (fetch) {\textbf{fetch FSM}\\ \footnotesize (\texttt{tst\_})};
\node[buf, right=1.9cm of fetch] (bsram) {BSRAM snapshot\\ \footnotesize $512\times16$, \texttt{0x1000}};
\node[blk, right=1.9cm of bsram] (cpu) {PicoRV32\\ \footnotesize firmware};
\node[blk, above=0.9cm of fetch] (acc) {audio accelerator\\ \footnotesize (after drain)};
\draw[d] (acc) -- node[al, right]{IRQ\\frame done} (fetch);
\draw[a] (sdram) -- node[al, above]{$20\times26$\\words} (fetch);
\draw[a] (fetch) -- node[al, above]{bin $=$\\burst$\cdot$26$+$idx} (bsram);
\draw[a] (bsram) -- node[al, above]{copy 512\\(bus, S0)} (cpu);
\draw[d] (cpu.north) to[out=160, in=20] node[al, above]{poll \texttt{fill\_cnt} (S0 \texttt{0x90})} (fetch.north east);
\end{tikzpicture}%
}
\caption{The read path. The accelerator's frame done IRQ does not interrupt the
CPU: in hardware it starts the fetch FSM, which reads the 20 row bursts back from
the SDRAM and unloads every word into a 512 word BSRAM at its deterministic bin
position. When the last burst lands, a frame counter (\texttt{fill\_cnt})
increments. The CPU polls that counter over the bus and, when it changes, copies
the snapshot, which stays frozen for the whole 10.9\,ms frame.}
\label{fig:fetch-path}
\end{figure}

The sequence per frame: the accelerator finishes its drain, releases the SDRAM
port and pulses the IRQ. The fetch FSM catches the edge, then for each of the 20
bursts it waits for the controller to be idle, issues one read strobe with the
row address of that burst, and sits in a fixed 200 cycle window collecting the
26 \texttt{rd\_valid} words (the 27th, defective, is dropped by the idx $<26$
gate). Each word is written into the BSRAM at address
$\mathrm{burst}\times 26 + \mathrm{idx}$, so the snapshot is filled
deterministically, bin by bin. After the last burst the FSM pulses
\texttt{frame\_ready}, the frame counter increments, and the machine parks until
the next IRQ. The whole fetch costs about $20\times205 \approx 4100$ cycles,
$\approx 100\,\mu$s of the 10.9\,ms frame.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    every node/.style={font=\footnotesize},
    st/.style={rounded rectangle, draw, line width=0.6pt, fill=white, minimum width=2.1cm, minimum height=0.8cm, align=center, inner sep=3pt},
    a/.style={-{Stealth}, line width=0.6pt},
    al/.style={font=\scriptsize, fill=white, inner sep=1pt}
]
\node[st] (init)  at (0,0)    {TS\_INIT};
\node[st] (rw)    at (4.8,0)  {TS\_READ\_WAIT};
\node[st] (rd)    at (9.6,0)  {TS\_READ};
\node[st] (pause) at (4.8,-2.6){TS\_PAUSE};
\draw[a] (init) -- node[al,above]{IRQ edge} (rw);
\draw[a] (rw) -- node[al,above]{\texttt{busy\_n}: strobe} (rd);
\draw[a] (rd.south west) to[bend left=20] node[al,below]{200 cycles, burst $<19$: next row} (rw.south east);
\draw[a] (rd.south) to[out=270,in=0] node[al,below right]{burst $=19$: \texttt{frame\_ready}} (pause.east);
\draw[a] (pause.west) to[out=180,in=270] (init.south);
\end{tikzpicture}
\caption{The fetch FSM. One loop iteration per burst: wait for the controller,
strobe the read with the burst's row address, collect words for a fixed 200 cycle
window while \texttt{rd\_valid} unloads into the BSRAM, then move to the next row.
After burst 19 it signals the frame and re-arms for the next IRQ.}
\label{fig:fetch-fsm}
\end{figure}

Why the BSRAM in the middle, instead of letting the CPU read the SDRAM words
directly? Because the direct version is what I built first, and it produced torn
spectra. The first S0 slave exposed the fetch FSM's 26 word burst buffer as bus
registers: the CPU was supposed to chase the FSM burst by burst, reading 26 words
inside each 200 cycle window. The CPU, which also has protocol work in its loop,
regularly lost the race; words from different bursts (and different frames) ended
up in the same ``spectrum'', and the decoder saw peaks that never existed. The
BSRAM snapshot removes the race instead of tightening it: hardware fills the
buffer deterministically, the buffer holds still for the whole frame, and the CPU
copies it whenever it gets around to it within the 10.9\,ms. The old burst window
is still mapped at offsets \texttt{0x00}--\texttt{0x64} as a diagnostic.

The same reasoning killed the interrupt. The accelerator's IRQ is physically wired
to the PicoRV32 (\texttt{irq[20]}), the startup code unmasks it and a full ISR
path exists in \texttt{crt0.S}; but the interrupt added nothing except a second
asynchronous path to debug, since at 91.6 frames per second a polled flag in the
main loop is serviced with milliseconds to spare. So the IRQ's production job is
to start the fetch FSM in hardware, and the firmware synchronises on
\texttt{fill\_cnt} alone.

\texttt{fill\_cnt} is a \emph{counter}, not a ready flag, and that is deliberate.
A flag must be cleared by the CPU, which means either a write back or a
clear-on-read register, and a clear-on-read is exactly the race that had already
bitten me on the UART (the valid bit cleared itself in the same cycle the
registered ack sampled it). A counter needs no clearing: the firmware remembers
the last value it saw and acts when the value read differs. Missing a frame is
harmless by construction, because the comparison still fails on the next poll and
the CPU only ever wants the \emph{latest} spectrum; and the snapshot it then
copies is guaranteed quiet, because a change of \texttt{fill\_cnt} means the fill
has \emph{just} finished, so the next one is a full 10.9\,ms away while the copy
takes a few hundred microseconds. Table~\ref{tab:s0map} is the resulting S0
register map.

\begin{table}[H]
\centering
\caption{S0 slave register map (base \texttt{0x4800\_0000}). The snapshot reads are
registered BSRAM accesses, acknowledged two cycles after the strobe; everything
else acks immediately.}
\label{tab:s0map}
\begin{tabular}{lll}
\toprule
\textbf{Offset} & \textbf{Dir} & \textbf{Function} \\
\midrule
\texttt{0x00}--\texttt{0x64} & R & current 26 word burst buffer (legacy / diagnostic) \\
\texttt{0x80} & R & fetch FSM state: [31] frame ready, [16] in read, [12:8] burst, [5:0] idx \\
\texttt{0x84} & R & counter: ADC samples seen \\
\texttt{0x88} & R & counter: FFT windows triggered \\
\texttt{0x8C} & R & counter: frame IRQs raised \\
\texttt{0x90} & R & \texttt{fill\_cnt}: completed snapshots (the flag the CPU polls) \\
\texttt{0x1000}--\texttt{0x17FC} & R & BSRAM snapshot, bin $i$ at \texttt{0x1000}$+4i$ (\texttt{xk\_re}, 16 bit) \\
\bottomrule
\end{tabular}
\end{table}

The three hardware counters at \texttt{0x84}--\texttt{0x8C} exist because, when the
chain silently stops, the first question is \emph{where}: if the sample counter
moves but the trigger counter does not, the capture side is broken; if triggers
move but IRQs do not, the FFT or drain side is. The firmware prints all three once
per second on the diagnostic channel (Section~\ref{sec:firmware}).

% =====================================================================
\section{Audio accelerator and tone decoder}
\label{sec:accel}
% =====================================================================

The receive path is built from two state machines that run in parallel and a
ping-pong buffer between them. The accelerator turns microphone samples into a
spectrum in SDRAM and raises the frame IRQ; from there the fetch FSM and the
BSRAM snapshot of Section~\ref{sec:fetch} carry the spectrum to the CPU, which
decodes the tones \emph{in software}, consistent with the design goal of keeping
the high level logic on the soft core. The CPU starts the accelerator once at
boot and never touches a raw sample. The capture FSM (\texttt{dma\_proc}) reads the
ADC over SPI and writes samples into \texttt{spi\_raw\_buf}, a 1024 word block RAM
holding two 512 sample windows. The FFT FSM (\texttt{fft\_proc}) takes the window
just filled, feeds it to the Gowin FFT core, collects the 512 spectrum words in
\texttt{fft\_result\_buf}, drains them to SDRAM through the dedicated write port
and raises the IRQ. While one half of \texttt{spi\_raw\_buf} is being processed,
the other half keeps filling, so no sample is lost.

Figure~\ref{fig:audio-zoom} is a zoom into the accelerator drawn in the same
visual language as the SoC diagram (Figure~\ref{fig:soc}): the same white blocks,
the same shaded block RAMs, the same hollow bus arrows, so it reads as the inset
of the \texttt{dma.vhd} container blown up to show its real internal modules and
its three external interfaces.

\begin{figure}[H]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
    every node/.style={font=\small},
    blk/.style={draw, line width=0.8pt, fill=white, minimum width=3.0cm, minimum height=1.5cm, align=center, text width=2.9cm},
    buf/.style={draw, line width=0.8pt, fill=black!7, minimum width=3.0cm, minimum height=1.5cm, align=center, text width=2.9cm},
    sip/.style={draw, line width=0.8pt, fill=white, minimum width=2.6cm, minimum height=1.5cm, align=center, text width=2.5cm},
    pin/.style={circle, draw, fill=white, line width=0.6pt, inner sep=1pt, minimum size=5pt},
    lbl/.style={font=\scriptsize},
    dp/.style={-{Stealth[length=6pt]}, line width=1.0pt},
    dpb/.style={{Stealth[length=6pt]}-{Stealth[length=6pt]}, line width=1.0pt},
    ctl/.style={-{Stealth[length=5pt]}, line width=0.7pt, densely dashed}
]
% ── fat hollow bus arrows (SoC style) ──────────────────────────────────────
\newcommand{\zR}[4]{\draw[fill=white, line width=0.8pt]
    (#1,#3+0.16) -- (#2-0.32,#3+0.16) -- (#2-0.32,#3+0.32) -- (#2,#3) --
    (#2-0.32,#3-0.32) -- (#2-0.32,#3-0.16) -- (#1,#3-0.16) -- cycle;
    \node[font=\scriptsize\sffamily] at ($(#1,#3)!0.5!(#2,#3)$) {#4};}
\newcommand{\zL}[4]{\draw[fill=white, line width=0.8pt]
    (#2,#3+0.16) -- (#1+0.32,#3+0.16) -- (#1+0.32,#3+0.32) -- (#1,#3) --
    (#1+0.32,#3-0.32) -- (#1+0.32,#3-0.16) -- (#2,#3-0.16) -- cycle;
    \node[font=\scriptsize\sffamily] at ($(#1,#3)!0.5!(#2,#3)$) {#4};}
\newcommand{\zBi}[4]{\draw[fill=white, line width=0.8pt]
    (#1+0.32,#3+0.16) -- (#2-0.32,#3+0.16) -- (#2-0.32,#3+0.32) -- (#2,#3) --
    (#2-0.32,#3-0.32) -- (#2-0.32,#3-0.16) -- (#1+0.32,#3-0.16) --
    (#1+0.32,#3-0.32) -- (#1,#3) -- (#1+0.32,#3+0.32) -- cycle;
    \node[font=\scriptsize\sffamily] at ($(#1,#3)!0.5!(#2,#3)$) {#4};}
\def\Bx{0.3}\def\Ble{2.4}\def\Sle{2.4}

% bus stub
\draw[fill=white, line width=1.0pt, rounded corners=2pt] (-0.3,-4.7) rectangle (0.3,4.0);
\node[rotate=90, font=\bfseries\sffamily\footnotesize] at (0,-0.4) {On chip bus};

% blocks
\node[blk] (dma) at (3.9,2.6) {\textbf{\texttt{dma\_proc}}\\capture FSM\\bus master M1};
\node[blk, minimum height=1.2cm] (cfg) at (3.9,-3.4) {\textbf{config regs}\\slave S5 (\texttt{0x30})\\start / stop / base};
\node[buf] (raw) at (8.2,2.6) {\texttt{spi\_raw\_buf}\\ping-pong\\$1024{\times}16$ BSRAM};
\node[blk] (fft) at (8.2,-1.4) {\textbf{\texttt{fft\_proc}}\\FFT + drain FSM};
\node[blk] (core) at (12.0,-1.4) {FFT 512\\Gowin IP core};
\node[buf] (res) at (12.0,2.6) {\texttt{fft\_result\_buf}\\$512{\times}16$ BSRAM};
\node[sip] (sip) at (15.6,2.6) {SDRC\_HS SIP\\\scriptsize dedicated write port};

% bus arrows: M1 (master, toward bus) and S5 (slave, toward block)
\zBi{\Bx}{\Ble}{2.6+0.6}{M1 data}
\zL{\Bx}{\Ble}{2.6}{M1 control}
\zL{\Bx}{\Ble}{2.6-0.6}{M1 address}
\node[lbl, anchor=south west] at (0.4,3.55) {to ADC SPI (S1) over the bus};
\zBi{\Bx}{\Sle}{-3.4+0.45}{S5 data}
\zR{\Bx}{\Sle}{-3.4}{S5 control}
\zR{\Bx}{\Sle}{-3.4-0.45}{S5 address}

% datapaths
\draw[dp] (dma.east) -- node[lbl, above]{12-bit sample} (raw.west);
\draw[dp] (raw.south) -- node[lbl, right]{window read} (raw.south |- fft.north);
\draw[dpb] (fft.east) -- node[lbl, above, align=center]{feed \texttt{xn\_re}\\collect \texttt{xk\_re}} (core.west);
\draw[dp] (core.north) -- node[lbl, right]{\texttt{xk\_re}} (core.north |- res.south);
\draw[dp] (res.east) -- node[lbl, above]{drain} (sip.west);

% control: trigger and config
\draw[ctl] (dma.south) |- (5.6,0.2) -| ([xshift=-7mm]fft.north);
\node[lbl, fill=white, inner sep=1pt] at (5.6,0.55) {\texttt{fft\_trigger} + \texttt{win\_base}};
\draw[ctl] (cfg.east) -- node[lbl, below]{start / base} ([yshift=-2mm]fft.west);

% external pins
\node[pin] (sdpin) at (17.6,2.6){}; \draw[dpb] (sip.east) -- (sdpin);
\node[lbl, anchor=west] at (sdpin.east) {to SDRAM die (8\,MB)};
\node[pin] (irq) at (8.2,-3.7){}; \draw[ctl] ([xshift=-6mm]fft.south) |- (irq);
\node[lbl, anchor=north] at ([yshift=-1pt]irq.south) {frame IRQ $\to$ fetch FSM / \texttt{irq[20]}};
\node[pin] (dbg) at (12.0,-3.7){}; \draw[dp, line width=0.7pt] ([xshift=6mm]fft.south) |- (dbg);
\node[lbl, anchor=north] at ([yshift=-1pt]dbg.south) {\texttt{fft\_ser} debug pin};

% container box
\begin{scope}[on background layer]
\node[draw, line width=1.1pt, fit=(dma)(cfg)(raw)(fft)(core)(res), inner sep=16pt, rounded corners=3pt] (box){};
\end{scope}
\node[anchor=south west, font=\bfseries] at (box.north west) {Audio accelerator \ \texttt{dma.vhd} \ (master M1 + slave S5)};
\end{tikzpicture}%
}
\caption{Audio accelerator, zoomed in the SoC style. The container is the
\texttt{dma.vhd} block of Figure~\ref{fig:soc} blown up to its real modules.
\texttt{dma\_proc} (the bus master M1) reads the ADC over the Wishbone bus and
writes 12-bit samples into the ping-pong \texttt{spi\_raw\_buf}; every 512 samples
it pulses \texttt{fft\_trigger} and hands the just filled window base to
\texttt{fft\_proc}. \texttt{fft\_proc} reads that window, feeds the Gowin FFT core,
collects the 512 spectrum words into \texttt{fft\_result\_buf}, then drains them to
SDRAM as 20 row bursts of 26 words through the SDRC\_HS SIP's \emph{dedicated write
port} (the one path that does not cross the bus, exactly as in
Figure~\ref{fig:soc}), and finally raises the frame IRQ. The config registers
(slave S5) only carry start/stop and the SDRAM output base; the dashed nets are
control, the solid ones are sample/spectrum datapaths. The whole right hand drain
never touches the bus, so it cannot collide with the CPU.}
\label{fig:audio-zoom}
\end{figure}

\subsection{Timing in cycles and in seconds}

Everything is paced by the ADC. One SCK period is the prescaler, 54 bus cycles, so
$54/40.5\,\mathrm{MHz} = 1.333\,\mu$s (SCK $=750$ kHz). One MCP3201 conversion is 16
SCK periods, so

\[
t_\mathrm{sample} = 16\times 54 = 864\ \text{cycles} = \frac{864}{40.5\,\mathrm{MHz}} = 21.33\,\mu\text{s},
\qquad f_s = \frac{1}{21.33\,\mu\text{s}} = 46.875\ \text{kHz}.
\]

One window is 512 samples, so the time to fill it (and the time between IRQs in
steady state) is

\[
t_\mathrm{window} = 16\times 512\times 54 = 442{,}368\ \text{cycles}
= \frac{442{,}368}{40.5\,\mathrm{MHz}} = 10.92\ \text{ms}
\quad\Rightarrow\quad \text{IRQ rate} = 91.6\ \text{Hz}.
\]

The processing of one window must fit inside that 10.92\,ms. Feeding 512 samples to
the FFT, the FFT pipeline, and collecting 512 outputs cost on the order of
$3\times 512 \approx 1500$ cycles at 40.5\,MHz, i.e. about $37\,\mu$s. The drain
itself is cheap: a warm up burst plus 20 bursts of 26 words, each burst about 30
cycles, is $\approx 650$ cycles $= 16\,\mu$s. What actually dominates is a
deliberate guard: before the first write strobe of every frame the FFT FSM sits
in a settle state of 200{,}000 cycles ($4.94$\,ms), a wait I copied verbatim from
the SDRAM bring up procedure that was proven on the bench (the first transaction
after a quiet period was the unreliable one, Section~\ref{sec:sdram}) and never
shrank, because the budget allows it. After the IRQ the FSM also streams the 512
words on a debug pin (8192 cycles, $202\,\mu$s). So

\[
t_\mathrm{process} \approx 0.04 + 4.94 + 0.02 + 0.20 \approx 5.2\ \text{ms}
\;<\; t_\mathrm{window} = 10.92\ \text{ms}
\qquad\left(\text{margin} \approx 2.1\times\right).
\]

This is the condition that makes the pipeline work: $t_\mathrm{process} <
t_\mathrm{window}$, so processing one window finishes before the next window is
full. The ping-pong buffer is what lets the two overlap. The margin is a
comfortable $2\times$ rather than a huge one, and almost all of it is the settle
guard: trimming that wait is the obvious first move if the budget ever tightens
(for example with a larger FFT), but as long as the margin holds, a wait that is
known to make the SDRAM reliable is not something I optimise away for elegance.

\subsection{The two state machines, drawn as HLSMs}

Both machines do real work in every state, not just sequencing, so I draw them as
high level state machines: each box keeps the RTL state name and lists the
register transfers that state performs, while the edge labels carry the guard and,
in braces, the action taken on the transition. The notation is the RTL one:
$\leftarrow$ is a clocked assignment, \texttt{WB} is a Wishbone access on the M1
master, \texttt{raw\_buf}/\texttt{res\_buf} are \texttt{spi\_raw\_buf} and
\texttt{fft\_result\_buf}.

\begin{figure}[H]
\centering
\resizebox{0.96\textwidth}{!}{%
\begin{tikzpicture}[
    every node/.style={font=\footnotesize},
    hb/.style={draw, line width=0.7pt, fill=white, align=left, inner sep=4pt, font=\scriptsize, text width=2.7cm, minimum height=1.5cm},
    a/.style={-{Stealth[length=5pt]}, line width=0.7pt},
    al/.style={font=\scriptsize, fill=white, inner sep=1.5pt}
]
\def\hh#1{\textbf{\texttt{#1}}\\[1.5pt]}
% feed-in (top left), then a clean rectangular steady-state loop on the right
\node[hb] (idle)  at (0,1.5)    {\hh{DS\_IDLE}\texttt{wr\_ptr}\,$\leftarrow$\,0};
\node[hb] (open)  at (3.4,1.5)  {\hh{DS\_OPEN}WB wr \texttt{SPI[0x04]}\,$\leftarrow$\,1\\(enable ADC SPI)};
\node[hb] (poll)  at (6.8,1.5)  {\hh{DS\_POLL}wait\\\texttt{spi\_data\_ready}};
\node[hb] (read)  at (10.2,1.5) {\hh{DS\_READ}WB rd \texttt{SPI[0x00]}\\\texttt{rdata}\,$\leftarrow$\,bus};
\node[hb] (store) at (6.8,-1.4) {\hh{DS\_STORE}\texttt{raw\_buf[ptr]} $\leftarrow$ \texttt{rdata}\\\texttt{wr\_ptr}++\\\scriptsize 512$^{\text{th}}$: \{\texttt{win\_base}, \texttt{trig}\}};
\node[hb] (clr)   at (10.2,-1.4){\hh{DS\_CLRDY}WB wr \texttt{SPI[0x0C]} $\leftarrow$ 0\\(clear ready)};
\draw[a] (idle) -- node[al,below]{\texttt{start\_r}} (open);
\draw[a] (open) -- (poll);
\draw[a] (poll) -- node[al,below]{ready} (read);
\draw[a] (read) -- (clr);
\draw[a] (clr)  -- (store);
\draw[a] (store) -- node[al,left]{always} (poll);
\end{tikzpicture}%
}
\caption{Capture HLSM (\texttt{dma\_proc}); each Wishbone access advances on its
\texttt{ack}, left implicit on the edges. After the one time \texttt{DS\_OPEN}
that enables the ADC SPI master, the machine loops
\texttt{POLL\,$\to$\,READ\,$\to$\,CLRDY\,$\to$\,STORE} once per sample: it waits
for the conversion, reads the word over the bus, clears the ready flag, and writes
the 12 low bits into the ping-pong buffer. Every 512$^{\text{th}}$ store it also
selects the just filled half (\texttt{win\_base} $=0$ or $512$) and pulses
\texttt{fft\_trigger}, which is the only thing it ever says to the FFT machine.}
\label{fig:capture-hlsm}
\end{figure}

\begin{figure}[H]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
    every node/.style={font=\footnotesize},
    hb/.style={draw, line width=0.7pt, fill=white, align=left, inner sep=4pt, font=\scriptsize, text width=2.95cm, minimum height=1.5cm},
    a/.style={-{Stealth[length=5pt]}, line width=0.7pt},
    al/.style={font=\scriptsize, fill=white, inner sep=1.5pt}
]
\def\hh#1{\textbf{\texttt{#1}}\\[1.5pt]}
% row 1 (feed/compute/collect), L->R
\node[hb] (idle) at (0,0)    {\hh{FS\_IDLE}\texttt{sdr\_own}\,$\leftarrow$\,0\\\texttt{rd\_idx}\,$\leftarrow$\,0};
\node[hb] (feed) at (4.4,0)  {\hh{FS\_FEED1}if \texttt{ipd}: \texttt{rd\_idx}++\\\texttt{xn\_re} $\leftarrow$\\\texttt{raw\_buf[base+idx]}};
\node[hb] (comp) at (8.8,0)  {\hh{FS\_COMPUTE}wait FFT\\pipeline (\texttt{busy})};
\node[hb] (coll) at (13.2,0) {\hh{FS\_COLLECT}if \texttt{opd}:\\\texttt{res\_buf[idx]} $\leftarrow$ \texttt{xk\_re}};
% row 2 (drain core), R->L
\node[hb] (settle) at (13.2,-3.4) {\hh{FS\_SDR\_SETTLE}if \texttt{initdone}:\\\texttt{settle}++\\(200k cycle guard)};
\node[hb] (ww) at (8.8,-3.4) {\hh{FS\_SDR\_WW}\texttt{data} $\leftarrow$ \texttt{res\_buf[b*26]}\\if \texttt{busy\_n}:\\\texttt{wr\_n}$\leftarrow$0, set \texttt{addr}};
\node[hb] (w) at (4.4,-3.4) {\hh{FS\_SDR\_W}\texttt{data} $\leftarrow$\\\texttt{res\_buf[b*26+i]}\\\texttt{widx}++ (29 cyc)};
% row 3 (irq/serial)
\node[hb] (irq) at (4.4,-6.6) {\hh{FS\_IRQ}\texttt{sdr\_own}$\leftarrow$0\\\texttt{sdr\_done}$\leftarrow$1\\\texttt{irq}$\leftarrow$1};
\node[hb] (ser) at (0,-6.6) {\hh{FS\_SERIAL}shift \texttt{res\_buf}\\$\to$ \texttt{fft\_ser}\\(512$\times$16 bit)};
% row 1 transitions
\node[al,align=center] at (2.2,1.15) {\texttt{trig}\,\&\,!\texttt{done} \{\texttt{start\_req}\}};
\draw[a] (idle) -- (feed);
\draw[a] (feed) -- node[al,above]{\texttt{eod}} (comp);
\draw[a] (comp) -- node[al,above]{\texttt{soud}} (coll);
\draw[a] (coll) -- node[al,right,align=left]{\texttt{eoud}\\\{\texttt{own}$\leftarrow$1\}} (settle);
% row 2 transitions (labels below, clear of boxes)
\draw[a] (settle) -- node[al,below]{=200k} (ww);
\draw[a] (ww) -- node[al,below]{\texttt{busy\_n}} (w);
\draw[a] (w.north) to[out=55,in=125] node[al,above]{\texttt{t}=28, next burst \{\texttt{b}++\} / warm done} (ww.north);
\draw[a] (w) -- node[al,right]{\texttt{b}=19} (irq);
\draw[a] (irq) -- (ser);
\draw[a] (ser.west) to[out=180,in=180,looseness=0.7] node[al,left,align=center]{512 words\\\{\texttt{done}$\leftarrow$0\}} (idle.west);
\end{tikzpicture}%
}
\caption{FFT and drain HLSM (\texttt{fft\_proc}). On a trigger it feeds the window
to the Gowin core one sample per \texttt{ipd} (\texttt{FS\_FEED1}), waits for the
pipeline (\texttt{FS\_COMPUTE}), and collects the 512 bins as they come out
(\texttt{FS\_COLLECT}). Then it takes the SDRAM write port and drains: a
\texttt{200{,}000} cycle settle guard copied from the proven bring up
(\texttt{FS\_SDR\_SETTLE}), then the warm up burst and 20 row bursts of 26 words,
each as a \texttt{WW}\,$\to$\,\texttt{W} present-then-strobe pair looping on
\texttt{b} (\texttt{FS\_SDR\_WW}/\texttt{FS\_SDR\_W}). After the last burst it
raises the IRQ and latches \texttt{sdr\_done} (\texttt{FS\_IRQ}), streams the frame
on a debug pin (\texttt{FS\_SERIAL}), and re-arms. The collect and drain states
touch no Wishbone, so the capture machine on M1 can never interrupt them.}
\label{fig:fft-hlsm}
\end{figure}

\begin{table}[H]
\centering
\caption{Audio accelerator control registers, base \texttt{0x3000\_0000}.}
\begin{tabular}{lll}
\toprule
\textbf{Offset} & \textbf{Dir} & \textbf{Function} \\
\midrule
\texttt{0x04} & W & start continuous capture + FFT \\
\texttt{0x08} & W & stop \\
\texttt{0x0C} & W & set SDRAM output base: \texttt{dat[20:0]} = 21 bit word address \\
\bottomrule
\end{tabular}
\end{table}

\subsection{FFT size and the tone bins}

The FFT is 512 point and runs on samples taken at 46.875\,kHz, so the bin spacing
is

\[
\Delta f = \frac{46875\ \mathrm{Hz}}{512} = 91.6\ \mathrm{Hz}.
\]

A tone at frequency $f$ lands in bin $k = \mathrm{round}(f / \Delta f)$. The five
tones the protocol uses, and the bins the decoder watches, are in
Table~\ref{tab:bins}.

\begin{table}[H]
\centering
\caption{Protocol tones, their FFT bins, and which node emits them. Master and slave
share the same 46.875\,kHz\,/\,512 grid ($\Delta f = 91.6$ Hz), so every tone is
\emph{on bin} in both directions and a single set of frequencies serves both. The
frequencies are the exact bin centres ($f = k\,\Delta f$) used in the code
(\texttt{emit\_tone.c} on the master, \texttt{global\_parameters.h} on the slave).
The two \emph{carriers} are distinct so each side knows who is speaking; the three
\emph{data} tones (EOF, bit 0, bit 1) are shared by both directions.}
\label{tab:bins}
\begin{tabular}{llll}
\toprule
\textbf{Tone} & \textbf{Frequency} & \textbf{Bin $k$} & \textbf{Emitted by} \\
\midrule
master carrier & 2014 Hz & 22 & master only \\
slave carrier  & 2472 Hz & 27 & slave only \\
EOF marker     & 2930 Hz & 32 & both \\
data bit 0     & 3571 Hz & 39 & both \\
data bit 1     & 4577 Hz & 50 & both \\
\bottomrule
\end{tabular}
\end{table}

The smallest gap between two used bins is between the EOF at bin 32 and bit 0 at
bin 39, that is 7 bins or 641\,Hz. That gap matters for the decoder, as explained
next.

\subsection{The tone decoder, with its checks}

The decoder ran first as a VHDL block (\texttt{audio\_decoder.vhd}); when the
soft core came up I ported it, check by check, to integer C
(\texttt{decoder.c}), and the VHDL original stays in the tree as a disabled
reference, so the two can be diffed. The checks below are therefore the same in
both worlds. For each of the five candidate bins, the decoder decides whether that tone is
present. It applies, in order: a local maximum test, the candidate bin magnitude
must be greater than or equal to its six neighbours within $\pm 3$ bins
($k\!-\!3 \dots k\!+\!3$); a curvature test on the three points $k\!-\!1, k, k\!+\!1$,
which must form a peak that opens downward (positive second difference), so that $k$
is genuinely a crest and not the skirt of a neighbour; and an amplitude test, the
parabolically interpolated peak must clear a fixed magnitude threshold. If all three
pass, the decoder picks the strongest data tone and the carrier, and emits the
character for that pair. The whole test is integer only (no division, no floating
point), so it is cheap on the soft core.

The $\pm 3$ neighbourhood, rather than a strict $\pm 1$, is used because the Hann
window applied before the FFT spreads a pure tone over two or three bins.

% =====================================================================
\section{SPI master for the ADC, and the microphone front end}
\label{sec:adc}
% =====================================================================

The receive chain in front of the FPGA is analog, and getting it described
correctly matters; it is the bottom of the board schematic (Figure~\ref{fig:board}),
and the whole chain runs off the FPGA's 5\,V rail, not the 3.3\,V. The electret
microphone produces a few millivolts riding on no particular DC level. It is AC
coupled through a 1\,$\mu$F capacitor and a 100\,$\Omega$ series resistor into an
MCP6022 operational amplifier, wired as a non inverting stage, which does two jobs:
it biases the signal to mid rail and it amplifies it. The bias comes from a
47\,k$\Omega$ / 47\,k$\Omega$ divider on the 5\,V rail, which sets the operating
point at $5/2 = 2.5\,\mathrm{V}$, the mid point of the ADC's $0$--$5$\,V input
range, so the amplified waveform swings symmetrically. The gain is set by the two
feedback resistors,

\[
G = 1 + \frac{R_2}{R_1} = 1 + \frac{100\,\mathrm{k}\Omega}{10\,\mathrm{k}\Omega} = 11,
\]

so a $\pm 4$\,mV microphone signal becomes a $\pm 44$\,mV swing around 2.5\,V, well
inside range. After the amplifier an RC low pass acts as an anti aliasing filter
before the ADC, with $R_f = 1.1\,\mathrm{k}\Omega$ and $C_f = 10\,\mathrm{nF}$, so
the corner is

\[
f_c = \frac{1}{2\pi R_f C_f} = \frac{1}{2\pi \cdot 1.1\,\mathrm{k}\Omega \cdot 10\,\mathrm{nF}}
    \approx 14.5\,\mathrm{kHz}.
\]

That corner is chosen to sit comfortably below the Nyquist frequency
($f_s/2 = 23.4\,\mathrm{kHz}$) while passing every protocol tone (the highest is
4577\,Hz), so it rejects the higher frequency content that would otherwise alias
into the band the decoder watches. The MCP3201 is a 12 bit \emph{SAR} (successive
approximation register) ADC: on each conversion it binary searches the input one
bit at a time against an internal DAC, and shifts the 12 bit result out over SPI.
It is the SPI slave (\texttt{Vref}\,$=5$\,V), and the FPGA SPI master clocks it to
read each conversion. So the chain is

\[
\begin{aligned}
\text{electret mic} &\rightarrow \text{1\,$\mu$F + 100\,$\Omega$} \rightarrow \text{MCP6022 ($G=11$, 2.5 V bias)} \rightarrow \text{RC anti alias (14.5 kHz)}\\
&\rightarrow \text{MCP3201 (12 bit SAR SPI ADC, Vref 5 V)} \rightarrow \text{FPGA SPI master}.
\end{aligned}
\]

\begin{table}[H]
\centering
\caption{SPI master (ADC) register map, base \texttt{0x4000\_0000} (offset in the low bits).}
\begin{tabular}{lll}
\toprule
\textbf{Offset} & \textbf{Dir} & \textbf{Function} \\
\midrule
\texttt{0x00} & R & 12 bit ADC sample; the read acks only when \texttt{data\_ready}=1 \\
\texttt{0x04} & W & start a conversion \\
\texttt{0x08} & W & stop \\
\texttt{0x0C} & W & clear the \texttt{data\_ready} flag \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Why a custom SPI master and not a generic one}

The MCP3201 has a fixed and slightly awkward frame: after the chip select goes low
there is a sample/null period, then the 12 data bits come out most significant
first. I wrote a dedicated SPI master for it rather than reuse a generic byte
oriented engine, because here I do not send anything (MOSI is unused), I only need
to clock 16 SCK periods, throw away the leading null bit, and capture exactly 12
bits into a register, then raise a data ready flag the accelerator polls. Tying the
frame to the part lets the sample rate be exact and constant, which the FFT needs.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  every node/.style={font=\scriptsize},
  sig/.style={line width=0.9pt},
]
\def\T{0.55}
% labels
\node[anchor=east] at (-0.3, 1.2) {CS};
\node[anchor=east] at (-0.3, 0.0) {SCK};
\node[anchor=east] at (-0.3,-1.2) {DOUT};
% CS: high, low for the frame, high again
\draw[sig] (-0.0,1.5) -- (0.4,1.5) -- (0.4,1.0) -- ({16*\T+0.4},1.0) -- ({16*\T+0.4},1.5) -- ({18*\T},1.5);
% SCK: 16 pulses starting at x=0.4
\foreach \i in {0,...,15} {
  \draw[sig] ({0.4+\i*\T},-0.2) -- ({0.4+\i*\T},0.2) -- ({0.4+\i*\T+0.5*\T},0.2) -- ({0.4+\i*\T+0.5*\T},-0.2) -- ({0.4+\i*\T+\T},-0.2);
}
% DOUT: null bit then B11..B0
\draw[sig] (0.0,-1.4) -- ({0.4+2*\T},-1.4);
\node[gray] at ({0.4+1.5*\T},-1.05) {null};
\foreach \i/\b in {0/{B11},2/{B9},4/{B7},6/{B5},8/{B3},10/{B1}} {
  \node[accentred] at ({0.4+(3.5+\i)*\T},-1.0) {\b};
}
\draw[accentred, sig] ({0.4+3*\T},-1.4) -- ({0.4+15*\T},-1.4);
% capture window bracket
\draw[<->] ({0.4+3*\T},0.75) -- ({0.4+15*\T},0.75);
\node[anchor=south] at ({0.4+9*\T},0.78) {12 data bits captured (B11\dots B0)};
\node[anchor=north, gray] at ({16*\T+0.4},0.95) {CS$\uparrow$: frame done};
\end{tikzpicture}
\caption{MCP3201 read as seen by the SPI master. CS goes low, the master issues 16
SCK periods at 750\,kHz, discards the leading null bit, captures the 12 data bits
B11 down to B0 most significant first, then raises CS to end the frame. One frame
is $16 / 750\,\mathrm{kHz} = 21.3\,\mu s$, which sets the 46.9\,kHz sample rate.}
\label{fig:spi-timing}
\end{figure}

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    every node/.style={font=\footnotesize},
    st/.style={rounded rectangle, draw, line width=0.6pt, fill=white, minimum width=2.0cm, minimum height=0.8cm, align=center, inner sep=3pt},
    a/.style={-{Stealth}, line width=0.6pt},
    al/.style={font=\scriptsize, fill=white, inner sep=1pt}
]
\node[st] (idle)  at (0,0)     {S\_IDLE};
\node[st] (lead)  at (3.4,0)   {S\_LEAD};
\node[st] (cap)   at (6.8,0)   {S\_CAPTURE};
\node[st] (latch) at (6.8,-2.4){S\_LATCH};
\node[st] (tail)  at (3.4,-2.4){S\_TAIL};
\draw[a] (idle)-- node[al,above]{start (0x04)}(lead);
\draw[a] (lead)--(cap);
\draw[a] (cap)-- node[al,right]{bit 13}(latch);
\draw[a] (latch)-- node[al,above]{\texttt{data\_ready}=1}(tail);
\draw[a] (tail) to[out=180,in=270] (idle.south);
\node[al, align=center] at (0,-2.0) {if start held:\\re-arm (next frame)};
\end{tikzpicture}
\caption{SPI master (ADC) frame FSM, from \texttt{spi\_master.vhd}. A write to
\texttt{0x04} asserts CS low and starts one MCP3201 frame of 16 SCK periods at
750\,kHz (\texttt{PRESCALER}=54 from the 40.5\,MHz clock). \texttt{S\_LEAD} clocks
the leading null bit, \texttt{S\_CAPTURE} shifts MISO in most significant first for
the 12 data bits, \texttt{S\_LATCH} writes them into the \texttt{0x00} register and
sets \texttt{data\_ready}, \texttt{S\_TAIL} raises CS to close the 21.3\,$\mu$s
frame. While \texttt{0x04} (start) stays asserted the frame re-arms, which gives the
continuous 46.9\,kHz sample stream the FFT needs; a write to \texttt{0x0C} clears
\texttt{data\_ready}.}
\label{fig:spi-fsm}
\end{figure}

% =====================================================================
\section{PWM speaker and LED}
\label{sec:speaker}
% =====================================================================

\begin{table}[H]
\centering
\caption{PWM register map (speaker base \texttt{0x5000\_0000}, LED base \texttt{0x6000\_0000}).
\texttt{F\_WIDTH} is the period field width.}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lll}
\toprule
\textbf{Offset} & \textbf{Dir} & \textbf{Function} \\
\midrule
\texttt{0x04} & W & load two tones and start: \texttt{dat[F\_WIDTH-1:0]}$=f_1$, \texttt{dat[2F\_WIDTH-1:F\_WIDTH]}$=f_2$ \\
\texttt{0x08} & W & stop \\
\bottomrule
\end{tabular}%
}
\end{table}

The transmit side is not a square wave generator, even though that would be the
simplest thing to do. A square wave at frequency $f$ carries all its odd harmonics
($3f, 5f, 7f, \dots$) at large amplitude; on the receiver those harmonics land in
other FFT bins and can be read as tones that were never sent, and they waste output
energy that should sit in the wanted bin. So the block (\texttt{pwm\_generic\_master})
synthesises the tone with direct digital synthesis (DDS) and a sine lookup table,
which keeps almost all of the energy at $f$.

The numbers. The speaker instance runs on the 40.5\,MHz bus clock (same domain as
the CPU that writes it, no clock domain crossing); the LED instance runs on
27\,MHz. A write to \texttt{0x04} loads two 16 bit
frequencies $f_1$ and $f_2$. Each one drives a 24 bit phase accumulator that adds a
phase increment every clock,
\[
  \Delta\phi = \mathrm{round}\!\left(\frac{f \cdot 2^{24}}{f_\mathrm{clk}}\right)
             = (f \cdot K + 2^{23}) \gg 24, \qquad
  K = \mathrm{round}\!\left(\frac{2^{48}}{f_\mathrm{clk}}\right),
\]
where the right hand form avoids a hardware divide. $K$ is a per instance generic,
precomputed because the synthesiser has no \texttt{math\_real}: $6{,}949{,}999$ for
the speaker at 40.5\,MHz, $10{,}424{,}999$ for the LED at 27\,MHz. The top 8 bits of
each accumulator index a 256 entry, 8 bit sine table whose entry
$i$ is $\mathrm{round}(128 + 127 \sin(2\pi i / 256))$, centred at 128. The two sine
samples are added and halved into one 8 bit duty value, so a single pin carries both
tones at once, which is exactly what the protocol needs since it sends superimposed
tone pairs.

That 8 bit duty feeds a free running 8 bit PWM comparator, so the carrier is
$f_\mathrm{clk} / 2^8$: $158.2$\,kHz on the speaker pin, $105.5$\,kHz on the LED
pin, both far above the audio band. The RC low
pass on the MOSFET gate then recovers the analog two tone sine and rejects the
carrier.

The amplitude is not switched on and off hard either. A hard on/off edge is a step,
which is broadband and would add a click plus spectral splatter into the neighbouring
bins. Instead an attack/release envelope ramps the AC part of the duty (the swing
around the 128 centre) from 0 to full and back over $\mathrm{RAMP\_MS} = 4\,\mathrm{ms}$,
one amplitude step every $\mathrm{RAMP\_TICK} = (f_\mathrm{clk}/1000 \cdot 4)/255$
clocks ($\approx 635$ at 40.5\,MHz). The phase accumulators keep running through the
release, so the tone fades out instead of cutting. The envelope is the small FSM in
Figure~\ref{fig:pwm-env}.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    every node/.style={font=\footnotesize},
    st/.style={rounded rectangle, draw, line width=0.6pt, fill=white, minimum width=2.0cm, minimum height=0.8cm, align=center, inner sep=3pt},
    a/.style={-{Stealth}, line width=0.6pt},
    al/.style={font=\scriptsize, fill=white, inner sep=1pt}
]
\node[st] (idle)   at (0,0)      {E\_IDLE};
\node[st] (attack) at (3.6,0)    {E\_ATTACK};
\node[st] (on)     at (7.2,0)    {E\_ON};
\node[st] (rel)    at (3.6,-2.4) {E\_RELEASE};
\draw[a] (idle)-- node[al,above]{start}(attack);
\draw[a] (attack)-- node[al,above]{\texttt{env\_q}=255}(on);
\draw[a] (on) to[out=270,in=0] node[al,right]{stop}(rel.east);
\draw[a] (rel)-- node[al,below]{\texttt{env\_q}=0}(idle);
\draw[a] (attack.south) to[bend right=22] node[al,left]{stop}(rel.north);
\draw[a] (rel.north) to[bend right=22] node[al,right]{start}(attack.south);
\end{tikzpicture}
\caption{Attack/release envelope FSM in the PWM block. \texttt{start} is set by the
\texttt{0x04} write and cleared by \texttt{0x08}. Attack ramps the amplitude up,
\texttt{E\_ON} holds it, release ramps it down; a stop during attack or a start
during release switches directly between the two ramps, so a retriggered tone never
clicks.}
\label{fig:pwm-env}
\end{figure}

The duty stream drives the gate of a low side MOSFET (an RLZ44N); the speaker sits
between the 12\,V rail and the MOSFET drain, with a flyback diode across it to absorb
the inductive kick when the MOSFET switches off. There is no operational amplifier in
the speaker path (the op amp belongs to the microphone input chain,
Section~\ref{sec:adc}): the chain is just PWM, RC filter, MOSFET, speaker.

The LED indicator is the 4 bit PWM peripheral, used the same way: its output drives
the gate of a second low side MOSFET that switches a 6\,W power LED on the 12\,V
rail, with a series resistor $R = (V_\mathrm{in}-V_f)/I_\mathrm{LED}$ setting the
current. The 4 bits give 16 brightness levels.

Who decides when a tone starts and stops is the firmware, not this block: the
soft core's tone player (Section~\ref{sec:firmware}) writes the two frequencies to
\texttt{0x04}, holds the tone for 144\,ms, writes \texttt{0x08}, and leaves
400\,ms of silence before the next symbol, so the receiver has time to finish
processing one symbol before the next arrives (a special warm up symbol holds the
tone for 300\,ms instead, Section~\ref{sec:protocol}).

% =====================================================================
\section{NOR flash storage: from hardware block to firmware driver}
\label{sec:flashctrl}
% =====================================================================

The flash subsystem has lived two lives. It started as \texttt{flash\_ctrl.vhd},
the largest VHDL block by line count and the one that caused the most trouble on
the bench: a self contained hardware state machine that took high level requests
from the ESP32 over a dedicated UART (append a 16 byte record, save the 32 byte
settings, read a slot, clear the log) and turned each one into the right sequence
of SPI commands to the external W25Q64 chip, with no CPU in the loop. Once the
soft core and its bus rules (Section~\ref{sec:wbrules}) proved solid, I retired
the hardware block and ported its state machine into firmware
(\texttt{norflash.c}): the ESP32 still speaks the same one character opcodes, but
they now land on a plain UART slave (S7, pins 72/20), the CPU runs the W25Q
sequences in C, and the SPI frames go out through the generic byte engine (S8,
pins 42/41/51/48). The protocol, the memory map and the SPI sequences below are
unchanged between the two lives; what changed is who issues them. The VHDL
original stays in the tree as a disabled reference, like the decoder and the
emitter before it.

\subsection{Memory layout}

I use the first 12\,KB of the chip, three 4\,KB sectors (4\,KB is the natural erase
granularity of the part). Sector 0 at \texttt{0x0000} holds the 32 byte settings:
a \texttt{0xA5} magic byte, a 15 byte device name, a 2 byte auto presence
interval, a 1 byte retry count, and, added with the display, a debug flag byte,
the three RGB theme colours (9 bytes), a view flags byte that selects what the
TFT zones show, and a second marker byte \texttt{0x5A} that distinguishes this
extended layout from the old one (a blob without it keeps safe defaults for the
new fields). Sector 1 at \texttt{0x1000} and sector 2 at
\texttt{0x2000} hold the log, 256 slots of 16 bytes each, so 512 slots together,
managed as a ring buffer. Each 16 byte record is: 2 bytes sequence number, 1 byte
type character (\texttt{B} boot, \texttt{R} registration, \texttt{P} presence,
\texttt{N} note, \texttt{E} event), 4 bytes timestamp, 1 byte value, 8 bytes ASCII
text. Figure~\ref{fig:flash-map} draws this map.

\begin{figure}[H]
\centering
\resizebox{0.92\textwidth}{!}{%
\begin{tikzpicture}[
  every node/.style={font=\footnotesize},
  sec/.style={draw, line width=0.9pt, fill=white, minimum width=8.0cm, align=center},
  cell/.style={draw, line width=0.6pt, fill=white, minimum height=0.85cm, align=center, inner sep=2pt},
  adr/.style={font=\scriptsize\ttfamily, anchor=east}
]
% three 4 KB sectors stacked
\node[sec, minimum height=1.2cm] (s0) at (0,4.0) {Sector 0 (4\,KB) \textbf{SETTINGS}};
\node[sec, minimum height=1.2cm] (s1) at (0,2.4) {Sector 1 (4\,KB) \textbf{LOG A}: slots 0\dots255};
\node[sec, minimum height=1.2cm] (s2) at (0,0.8) {Sector 2 (4\,KB) \textbf{LOG B}: slots 256\dots511};
\node[adr] at (-4.3,4.0) {0x0000};
\node[adr] at (-4.3,2.4) {0x1000};
\node[adr] at (-4.3,0.8) {0x2000};
% ring buffer arrow (512 slots wrap)
\draw[-{Stealth}, line width=0.7pt] (s2.east) to[out=0,in=0,looseness=1.6] (s1.east);
\node[font=\scriptsize, anchor=west] at (4.6,1.6) {ring: 512 slots wrap};
% zoom: one 16-byte record, fields cut into cells
\node[font=\footnotesize] at (0,-0.7) {one 16 byte record (one slot):};
\node[cell, minimum width=1.4cm] (f1) at (-3.5,-1.6) {seq\\2 B};
\node[cell, minimum width=1.0cm] (f2) at (-2.1,-1.6) {type\\1 B};
\node[cell, minimum width=2.0cm] (f3) at (-0.6,-1.6) {t\_sec\\4 B};
\node[cell, minimum width=1.0cm] (f4) at (0.9,-1.6) {val\\1 B};
\node[cell, minimum width=3.0cm] (f5) at (2.9,-1.6) {text (ASCII)\\8 B};
% zoom: settings fields (v2: + debug, colori, view flags, marker 0x5A)
\node[font=\footnotesize] at (0,-3.0) {settings (sector 0, first 31 B used):};
\node[cell, minimum width=1.1cm] (g1) at (-4.6,-3.9) {magic\\\texttt{0xA5}};
\node[cell, minimum width=2.4cm] (g2) at (-2.8,-3.9) {device name\\15 B};
\node[cell, minimum width=1.3cm] (g3) at (-0.9,-3.9) {auto\\2 B};
\node[cell, minimum width=1.0cm] (g4) at (0.3,-3.9) {retry\\1 B};
\node[cell, minimum width=1.0cm] (g5) at (1.3,-3.9) {dbg\\1 B};
\node[cell, minimum width=1.9cm] (g6) at (2.8,-3.9) {colours RGB\\$3{\times}3$ B};
\node[cell, minimum width=1.2cm] (g7) at (4.4,-3.9) {views\\1 B};
\node[cell, minimum width=1.1cm] (g8) at (5.6,-3.9) {v2\\\texttt{0x5A}};
\end{tikzpicture}%
}
\caption{Flash memory map (low 12\,KB). Three 4\,KB sectors: sector 0 holds the 32
byte settings, sectors 1 and 2 hold the 512 slot log ring buffer. Below, the 16 byte
record and the settings sector are cut into their byte fields; the debug flag, the
TFT theme colours, the view flags and the \texttt{0x5A} v2 marker are the fields
added with the display.}
\label{fig:flash-map}
\end{figure}

\subsection{The state machine, and its firmware mirror}

Figure~\ref{fig:flashctrl-fsm} is the state machine as it existed in
\texttt{flash\_ctrl.vhd}; the firmware mirrors it almost mechanically. The boot
chain on the top row became \texttt{nor\_init()}, the IDLE hub became the
\texttt{nor\_poll()} call in the main loop (one non blocking peek at the S7 UART
per pass), and each opcode handler on the bottom row became a \texttt{case} in
its dispatch \texttt{switch}. Porting a state machine that was already debugged
in hardware meant the firmware worked against the real chip almost immediately;
the bugs that did appear were all in the bus plumbing, and they are the ones
that produced the rules of Section~\ref{sec:wbrules}.

\begin{figure}[H]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
    every node/.style={font=\footnotesize},
    st/.style={rounded rectangle, draw=boxstroke, fill=boxbg, minimum width=2.0cm, minimum height=0.8cm, align=center, inner sep=3pt},
    acc/.style={rounded rectangle, draw=accentgreen!60!black, fill=accentgreen!18, minimum width=2.0cm, minimum height=0.8cm, align=center, inner sep=3pt},
    a/.style={-{Stealth}, line width=0.7pt},
    b/.style={{Stealth}-{Stealth}, line width=0.7pt},
    al/.style={font=\scriptsize, fill=white, inner sep=1pt}
]
% boot init chain (top), left to right, then down into IDLE
\node[st] (boot) at (0,0)    {BOOT};
\node[st] (wrsr) at (3.2,0)  {WRSR};
\node[st] (gbu)  at (6.4,0)  {GBU 0x98};
\node[st] (rd)   at (9.6,0)  {RDSR};
\node[st] (rep)  at (12.8,0) {REPORT 'S'+SR};
\node[st] (scan) at (16.0,0) {SCAN 512};
\draw[a] (boot)--(wrsr);
\draw[a] (wrsr)-- node[al,above]{poll}(gbu);
\draw[a] (gbu)-- node[al,above]{poll}(rd);
\draw[a] (rd)--(rep);
\draw[a] (rep)--(scan);
% IDLE hub
\node[acc] (idle) at (8.0,-2.4) {IDLE};
\draw[a] (scan) |- (idle);
% opcode handlers (bottom row); bidirectional arrow = enter on char, return when done
\node[st] (ap) at (0,-4.8)    {AP\_* (L)};
\node[st] (sw) at (2.3,-4.8)  {SW\_* (S)};
\node[st] (cl) at (4.6,-4.8)  {CL\_* (C)};
\node[st] (g)  at (6.9,-4.8)  {G\_RD (G)};
\node[st] (h)  at (9.2,-4.8)  {H\_SEND (H)};
\node[st] (q)  at (11.5,-4.8) {Q\_RD (Q)};
\node[st] (i)  at (13.8,-4.8) {I\_RD (I)};
\node[st] (t)  at (16.0,-4.8) {T\_RD (T)};
\draw[b] (idle)-- node[al,sloped,above]{'L'}(ap);
\draw[b] (idle)-- node[al,sloped,above]{'S'}(sw);
\draw[b] (idle)-- node[al,sloped,above]{'C'}(cl);
\draw[b] (idle)-- node[al,left]{'G'}(g);
\draw[b] (idle)-- node[al,right]{'H'}(h);
\draw[b] (idle)-- node[al,sloped,above]{'Q'}(q);
\draw[b] (idle)-- node[al,sloped,above]{'I'}(i);
\draw[b] (idle)-- node[al,sloped,above]{'T'}(t);
\end{tikzpicture}%
}
\caption{The storage state machine (drawn from \texttt{flash\_ctrl.vhd}; the
firmware port keeps the same shape). At power up it unlocks the chip (WRSR clears the
protection bits BP0..BP2 and SRP and sets QE, then a Global Block Unlock), reads the
status register back, reports it unsolicited over the UART (\texttt{'S'} plus the SR
byte; in practice the ESP32 boots too late to catch it and asks again with
\texttt{T}), and scans all 512 slots to find the head of the ring buffer, then sits
in IDLE. Each UART opcode (\texttt{L} append, \texttt{S} save settings, \texttt{C}
clear, \texttt{G} read slots, \texttt{H} read head, \texttt{Q} read settings,
\texttt{I} JEDEC id, \texttt{T} read status register) is entered from IDLE on its
character and returns to IDLE when its SPI sequence finishes; the double headed
arrows mean exactly that go and return.}
\label{fig:flashctrl-fsm}
\end{figure}

\paragraph{Append (opcode L) as an SPI sequence.} An append is not a single command
but a short ordered sequence of SPI frames to the chip, with a status poll between
the slow ones. Figure~\ref{fig:flash-append} is that sequence in time order.

\begin{figure}[H]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
  every node/.style={font=\footnotesize},
  cmd/.style={draw, line width=0.7pt, fill=white, minimum height=1.0cm, align=center, inner sep=4pt},
  poll/.style={draw, dashed, line width=0.7pt, fill=white, minimum height=1.0cm, align=center, inner sep=4pt},
  a/.style={-{Stealth}, line width=0.7pt}
]
\node[cmd]  (we1) at (0,0)    {WE\\\texttt{0x06}};
\node[cmd]  (er)  at (2.6,0)  {SECTOR ERASE\\\texttt{0x20}+A[23:0]};
\node[poll] (p1)  at (5.6,0)  {poll RDSR \texttt{0x05}\\until WIP=0};
\node[cmd]  (we2) at (8.6,0)  {WE\\\texttt{0x06}};
\node[cmd]  (pp)  at (11.4,0) {PAGE PROGRAM\\\texttt{0x02}+A[23:0]+16\,B};
\node[poll] (p2)  at (14.6,0) {poll RDSR\\until WIP=0};
\node       (done)at (16.9,0) {send \texttt{'K'}};
\draw[a] (we1)--(er); \draw[a] (er)--(p1); \draw[a] (p1)--(we2);
\draw[a] (we2)--(pp); \draw[a] (pp)--(p2); \draw[a] (p2)--(done);
\draw[a] (p1.north) to[out=60,in=120,looseness=4] node[above,font=\scriptsize]{WIP=1}(p1.north);
\draw[a] (p2.north) to[out=60,in=120,looseness=4] node[above,font=\scriptsize]{WIP=1}(p2.north);
\node[font=\scriptsize, align=center] at (2.6,-1.2) {only if target\\slot $\neq$ \texttt{0xFF}};
\end{tikzpicture}%
}
\caption{Append (opcode \texttt{L}) as the time ordered sequence of SPI frames the
controller sends to the W25Q. Each solid box is one CS low frame; the dashed boxes
are status polls (read \texttt{0x05}, loop while the WIP write in progress bit is 1).
The controller first probes the target slot; the erase happens only if that slot is
not \texttt{0xFF}, because a NOR program can only turn 1 bits into 0, so writing over
a used slot would corrupt it. A read (opcode \texttt{G}) is instead a single frame:
\texttt{0x03} + 24 bit address, then clock out $N\times16$ data bytes.}
\label{fig:flash-append}
\end{figure}

\subsection{SCAN: finding the head after power up}

At power up the controller does not know where the last valid record is. It reads
byte 2 (the type byte) of every one of the 512 slots and treats a slot as valid only
if that byte is one of \texttt{B, R, P, N, E}. Anything else (the \texttt{0xFF} of an
erased slot, or garbage from an aborted write) counts as empty. The head becomes
(highest valid slot $+ 1$) mod 512. This is more robust than stopping at the first
\texttt{0xFF}, which I tried first: that version stopped at any hole, even when valid
records sat past the hole, which is exactly what a failed retry leaves behind.

\subsection{Unlocking the chip, strictly first}

The boot order is not negotiable, and the reason is a hardware one. On my board
the \texttt{/HOLD\#} and \texttt{/WP\#} pins of the W25Q are unconnected, and a
floating \texttt{/HOLD\#} picks up every SCK edge capacitively: the chip pauses
itself mid frame, MISO floats, and \emph{every} read returns \texttt{0xFF},
including the JEDEC id. So the intuitive plan, ``read the id first to check the
chip is there'', cannot work: the very first frames at boot must be the blind
write enable plus WRSR that sets QE$=1$ in status register 2 (QE disables the
\texttt{/HOLD\#}/\texttt{/WP\#} pin functions on this part) and clears the
protection bits in status register 1, then the Global Block Unlock
(\texttt{0x98}). Only after that does the chip answer, and only then does the
firmware read the JEDEC id to set its \emph{presence} flag (first byte
\texttt{0xEF} $=$ Winbond). If the id is wrong the flag stays low and every
later storage opcode fails fast with \texttt{'E'} instead of hammering timeouts.

The timeouts themselves changed nature in the port. The hardware counted
cycles (25 million, $\approx 620$\,ms); the firmware counts \emph{time} on
\texttt{rdcycle}, because a C loop iteration has no fixed duration: 2\,ms per
SPI byte transfer (a transfer at 633\,kHz takes 13\,$\mu$s, so this only trips
if the engine is dead), 700\,ms on the WIP poll (a sector erase can take
400\,ms), 200\,ms per payload byte from the ESP32 (a truncated command aborts
without acknowledging, and the ESP32's own retry takes over). A dead SPI or an
absent chip therefore costs a bounded few seconds at boot, not a hang.

\subsection{The bus side: S7 commands in, S8 frames out}

The two slaves that replaced \texttt{flash\_ctrl} are deliberately dumb. S7 is a
second instance of the same UART generic used for the characters, on the same
FPGA pins (72/20) the hardware block used, so the ESP32 did not change a wire or
a byte of its protocol. S8 is the generic SPI byte engine: one byte per
transfer, chip select held by a register bit across bytes so a multi byte W25Q
frame is just a CS assert, $n$ TXDATA writes, a CS release. Its \texttt{done}
flag is persistent, cleared by the next TXDATA write rather than by reading
STATUS, for the reason of Section~\ref{sec:wbrules}; and the S7 UART is the
peripheral that motivated the 16 entry receive FIFO, because a 16 byte append
payload arrives back to back at 115200 baud while the CPU may be mid way through
a display chunk or a spectrum copy.

\begin{table}[H]
\centering
\caption{Generic SPI engine register map (slave S8, base \texttt{0x3800\_0000}).}
\begin{tabular}{lll}
\toprule
\textbf{Offset} & \textbf{Dir} & \textbf{Function} \\
\midrule
\texttt{0x00} & R & RXDATA, the last received byte \\
\texttt{0x04} & W & TXDATA, write a byte and start a full duplex transfer (clears \texttt{done}) \\
\texttt{0x08} & W & CTRL, bit 0 = chip select (1 asserts CS low, held across bytes) \\
\texttt{0x0C} & R & STATUS, bit 0 = busy, bit 1 = done (non destructive read) \\
\bottomrule
\end{tabular}
\end{table}

One behavioural addition came free with the port: the firmware applies the
settings it loads. At boot, and again on every \texttt{S} save, it parses the 32
byte blob and immediately adopts the debug flag (which gates all the \texttt{\$}
diagnostic traffic, so verbosity is now a dashboard switch instead of a
recompile), the TFT theme colours and the view flags; a settings epoch counter
ticks on every apply, and the display manager redraws when it sees it change
(Section~\ref{sec:display}). The boot also prints a one line summary on the
diagnostic channel (\texttt{\$NOR ok id=EF hd=... lk=0}), which replaced a whole
debugging session's worth of guessing about the state of the chip.

% =====================================================================
\section{The TFT display: a fourth SPI and a mini dashboard}
\label{sec:display}
% =====================================================================

Until this point the only way to see what the master was doing was the UART
console or the web dashboard, both of which need a laptop. The last addition is
a 2 inch IPS panel (a GMT020-02-7P, ST7789 controller, $240\times320$) wired
straight to the FPGA, on which the soft core draws its own mini dashboard. It
needs five wires, on five spare FPGA pins (27 to 31): SCL, SDA, DC, RST and CS.

\subsection{The \texttt{spi\_display} slave}

The panel is driven by a fourth SPI master, and it is the simplest of the four
because the ST7789 in this wiring never talks back: \texttt{spi\_display.vhd}
is transmit only (no MISO pin at all). It runs SPI mode 3, which is what the
panel expects: SCK idles \emph{high}, the data line changes on the falling edge
and the panel samples it on the rising edge, MSB first. The prescaler is
$\div6$, so SCK is $40.5/6 = 6.75$\,MHz, by far the fastest SPI in the design;
it can afford to be, because the panel sits centimetres from the FPGA on its
own wires, with none of the shared ground drama that capped the NOR at
633\,kHz. The three panel control pins are register bits: DC (the ST7789
command/data select), RST (the panel reset) and CS. Being the newest slave, it
was written with the Section~\ref{sec:wbrules} rules from the first line: ack
held while the strobe is up, and a busy flag whose read is non destructive.

\begin{table}[H]
\centering
\caption{\texttt{spi\_display} register map (slave S9, base \texttt{0x5800\_0000}).}
\begin{tabular}{lll}
\toprule
\textbf{Offset} & \textbf{Dir} & \textbf{Function} \\
\midrule
\texttt{0x00} & W & TXDATA: write a byte and start the shift (ignored while busy) \\
\texttt{0x04} & W & CTRL: bit 0 = DC (0 command, 1 data), bit 1 = RST pin, bit 2 = CS assert \\
\texttt{0x08} & R & STATUS: bit 0 = busy (non destructive read) \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    every node/.style={font=\footnotesize},
    st/.style={rounded rectangle, draw, line width=0.6pt, fill=white, minimum width=2.0cm, minimum height=0.8cm, align=center, inner sep=3pt},
    a/.style={-{Stealth}, line width=0.6pt},
    al/.style={font=\scriptsize, fill=white, inner sep=1pt}
]
\node[st] (idle) at (0,0)   {IDLE\\ \tiny SCK high};
\node[st] (lo)   at (4.2,0) {DRIVE\\ \tiny SCK low, bit on SDA};
\node[st] (hi)   at (8.4,0) {SAMPLE\\ \tiny SCK high, panel reads};
\draw[a] (idle) -- node[al,above]{TXDATA write} (lo);
\draw[a] (lo) -- node[al,above]{$\div6$ half period} (hi);
\draw[a] (hi.south) to[bend left=25] node[al,below]{bit $<7$: shift, next bit} (lo.south);
\draw[a] (hi.north) to[bend right=20] node[al,above]{bit $=7$: done} (idle.north);
\end{tikzpicture}
\caption{The \texttt{spi\_display} shifter. A TXDATA write loads the byte and
pulls SCK low with the MSB already on SDA; each bit then spends half a $\div6$
period low (data driven) and half high (panel samples, mode 3); after the
eighth bit SCK parks high, which \emph{is} the mode 3 idle, so byte boundaries
need no extra states.}
\label{fig:disp-fsm}
\end{figure}

\subsection{Bring up: the ST7789 init sequence}

After releasing the panel reset (RST low 20\,ms, then high, then 150\,ms of
patience), the firmware sends the seven command init that the datasheet
prescribes, each with its mandated delay: SWRESET (150\,ms), SLPOUT (120\,ms),
COLMOD $=$ \texttt{0x55} (16 bit RGB565 pixels), MADCTL $=$ \texttt{0x60}
(landscape, so the $240\times320$ panel becomes $320\times240$), INVON (this
IPS panel wants inverted polarity, without it colours come out negative), NORON
and DISPON. From then on drawing is just three commands: CASET and RASET to
declare a rectangular window, RAMWR followed by $w\times h$ RGB565 pixels.

\subsection{\texttt{display\_manager}: drawing without blocking}

A full screen is $320\times240\times2 = 153{,}600$ bytes, which at 6.75\,MHz is
$\approx180$\,ms of solid pushing. The main loop cannot disappear for 180\,ms:
the tone player would stutter and, worse, the S7 FIFO would overflow mid
append. So the display manager never draws synchronously. Drawing requests
become \emph{jobs} (fill this rectangle, write this text) in a 48 entry queue,
and each \texttt{display\_tick()} call executes at most one small chunk of the
current job: 64 pixels of a fill, or one glyph of a text (a $5\times7$ font,
scaled in integer steps). A chunk is 128 to 384 SPI bytes, $150$--$450\,\mu$s,
which is invisible next to the 10.9\,ms audio frame; the screen simply
assembles itself over a few dozen loop passes.

\begin{figure}[H]
\centering
\begin{tikzpicture}[scale=0.022, every node/.style={font=\scriptsize}]
\draw[line width=1.0pt] (0,0) rectangle (320,240);
\draw[line width=0.7pt] (96,0) -- (96,240);
\draw[line width=0.7pt] (224,0) -- (224,240);
\draw[line width=0.7pt] (96,120) -- (224,120);
\node[align=center] at (48,120) {\textbf{zone 0}\\SLAVES\\\emph{or} TX PKT};
\node[align=center] at (160,180) {\textbf{zone 1}\\spectrum\\\emph{or} PEAKS};
\node[align=center] at (160,60) {\textbf{zone 2}\\LOGS};
\node[align=center] at (272,120) {\textbf{zone 3}\\RX PKT\\\emph{or} IRQ count};
\node[anchor=south west, font=\tiny] at (2,242) {$320\times240$ landscape, RGB565};
\end{tikzpicture}
\caption{The four fixed zones of the on screen dashboard. What each zone shows
is chosen by the view flags byte of the NOR settings (each zone has a priority
list, first enabled flag wins), and the three theme colours come from the same
settings, so the dashboard restyles the TFT remotely; a save bumps the settings
epoch and the display redraws from scratch.}
\label{fig:disp-zones}
\end{figure}

The zone contents are all data the firmware already had. SLAVES renders the
health mask of Section~\ref{sec:firmware} as a green/red OK/KO list, one row
per bus slave plus the NOR presence flag. The spectrum view draws one bar per
FFT bin in the band the decoder cares about (bins 16 to 55), straight out of
the same \texttt{g\_sdram[]} snapshot the decoder reads, with the protocol bins
in the accent colour, so the panel shows the tones arriving in real time; PEAKS
is its numeric twin (the two strongest bins with their frequency and the symbol
they would decode to). RX and TX show the last decoded character and counters
from the emitter; IRQ shows the accelerator frame counter; LOGS is a six line
ring of short firmware events (NOR appends, clears, settings saves). Beyond the
init delays, the manager is a flat round robin: one zone's content is marked
dirty every 1.5\,s and redrawn in chunks, and a settings epoch change marks
everything dirty at once.

% =====================================================================
\section{Signal integrity and decoupling}
\label{sec:si}
% =====================================================================

None of the problems in this section showed up in simulation. All of them showed up
on the bench, and the fixes are a mix of a few capacitors, a slower clock, and
firmware retries. The common thread is that the W25Q64 sits on a breakout board
connected to the FPGA by about 10\,cm of Dupont jumper wires, which is a poor
environment for both power delivery and signal edges.

\subsection{Brownout during sector erase}

To erase a 4\,KB sector the chip runs an internal charge pump that draws around
80\,mA in peaks for about 30\,ms (typical), up to 400\,ms worst case. To program one
record the draw is lighter, around 25\,mA for under 3\,ms. The supply reaches the
chip through the jumper wires, so any current spike shows up as a voltage droop at
the chip.

The droop over a decoupling capacitor follows $\Delta V = I\,t/C$. With no capacitor
at all (only the tens of pF of the wire), the 80\,mA erase current collapsed the
3.3\,V rail almost instantly. The chip browned out mid erase, lost its write enable,
silently rejected the program that followed, and the firmware saw a success that had
not happened.

Adding a 1\,$\mu$F ceramic across VCC and GND at the chip gives

\[
\Delta V = \frac{0.080 \times 0.030}{1\times 10^{-6}} = 2.4\ \mathrm{V},
\]

which still drops the rail to 0.9\,V, below the 2.7\,V minimum, so log writes started
working but settings saves (erase plus program) still failed. Adding a second
capacitor of 10\,$\mu$F in parallel, for 11\,$\mu$F total, gives

\[
\Delta V = \frac{0.080 \times 0.030}{11\times 10^{-6}} = 0.22\ \mathrm{V},
\]

so the rail only drops to 3.08\,V, safely above 2.7\,V, and the brownout is gone. The
two values cover different bands: the 1\,$\mu$F has low ESR/ESL and handles the fast
SPI edges, the 10\,$\mu$F holds up the slow charge pump current. The minimum
capacitance to stay inside a 0.6\,V budget is
$C \ge I\,t/\Delta V = 0.080\times0.030/0.6 = 4\,\mu$F, so 11\,$\mu$F has comfortable
margin.

\subsection{SPI clock too fast for the wiring}

Separately, the first SPI clock of 5\,MHz was too fast for 10\,cm of jumper with a
single ground return. Long writes came out truncated: a 16 byte record would arrive
with byte 10 onward stuck at \texttt{0xFF}. I lowered the clock in steps and watched
the first attempt success rate: 5\,MHz was bad, 2.5\,MHz better, 1.27\,MHz mostly
good, and 633\,kHz (divide by 64) almost always good. At 633\,kHz a bit lasts
1.58\,$\mu$s, long enough that the ringing on the wire settles well before the chip
samples.

\subsection{Retry and verify in firmware}

Even at 633\,kHz an occasional write still fails (roughly 1 in 100 on settings,
rarer on log records). The firmware does up to 5 attempts: send, wait for the
acknowledgement, read the slot back, compare all 16 bytes, retry if they differ. A
failed attempt burns one ring buffer slot, but the SCAN at boot ignores those
because their type byte is not in the valid set, so nothing is lost permanently.
Table~\ref{tab:bench} is the measured reliability after all three fixes.

\begin{table}[H]
\centering
\caption{Measured first attempt reliability after 633\,kHz SPI, 11\,$\mu$F
decoupling and firmware retry/verify.}
\label{tab:bench}
\begin{tabular}{lll}
\toprule
\textbf{Operation} & \textbf{First attempt} & \textbf{After up to 5 retries} \\
\midrule
log record write (16 B)              & $\approx 95\%$ & $\approx 100\%$ \\
settings save (32 B, with erase)     & $\approx 85\%$ & $\approx 100\%$ \\
read back of a record                & $100\%$        & $100\%$ \\
JEDEC ID read at boot                & $100\%$        & $100\%$ \\
\bottomrule
\end{tabular}
\end{table}

A separate hardware lesson, worth recording: an indicator LED that I had wired with
its anode straight to an FPGA pin and its cathode to the common ground, with no
series resistor, was injecting tens of mA into the ground return. That current,
through the impedance of the shared ground wire, moved the ground reference enough
to corrupt the microphone ADC readings (the FFT then "heard" tones that were not
there) and to disturb the UART and SPI at the same time. Removing the resistorless
LEDs fixed all of it at once. The rule is to always put a series resistor (330\,$\Omega$
to 1\,k$\Omega$) on an LED driven from a logic pin.

% =====================================================================
\section{Soft core firmware}
\label{sec:firmware}
% =====================================================================

The firmware on the PicoRV32 is still compact: seven C files and a startup
assembly file, compiled with the RISC-V GCC toolchain for \texttt{rv32imc} at
\texttt{-O1}. The linker script places code and constants in the 32\,KB ITCM at
\texttt{0x0200\_0000} and data, BSS and stack in the 16\,KB DTCM at
\texttt{0x0100\_0000}; both are CPU local block RAM, off the Wishbone. The build
ends with a small script that flattens the binary into \texttt{ram32.hex}, one 32
bit word per line, which is the format the Gowin PicoRV32 IP wants as its ITCM
initialisation file; the firmware therefore travels inside the bitstream, and
changing the firmware means regenerating the IP. (My first attempts handed the IP
the Intel HEX file that GCC produces, which it accepts silently and loads as
garbage; the flat format is the one that works.)

The structure is a single main loop with no operating system and, deliberately, no
interrupts (Section~\ref{sec:fetch}): each pass calls the transmit side
(\texttt{emit\_capture}, \texttt{emit\_player\_tick}), the receive side
(\texttt{decode\_step}), the NOR command dispatcher (\texttt{nor\_poll},
Section~\ref{sec:flashctrl}), the bus health scan (\texttt{health\_tick}, below),
the display renderer (\texttt{display\_tick}, Section~\ref{sec:display}) and,
when the debug flag is set, the diagnostic dumps. Every function is non
blocking, so each loop pass is short and every flag gets polled within
microseconds; the two costliest passes, a 512 word spectrum copy and a display
chunk, are each a few hundred microseconds and cannot happen back to back often
enough to matter. The debug flag itself is no longer a compile time constant:
it is bit \texttt{b[19]} of the NOR settings, loaded at boot and re-applied
live on every save, so verbosity is toggled from the web dashboard without
touching the toolchain.

\subsection{Transmit: from UART character to tone pair}

\texttt{emit\_capture} peeks the character UART (S6): if a received byte is one of
the protocol characters (\texttt{a}--\texttt{d}, \texttt{A}--\texttt{D}) it is
pushed into a 16 entry software queue. \texttt{emit\_player\_tick} is the player,
a three state machine. In \texttt{P\_IDLE} it pops a character, maps it to the
tone pair (the master carrier at 2014\,Hz in the low half word, the data tone in
the high half word), writes the pair to the PWM start register and moves to
\texttt{P\_TONE}; after 144\,ms it writes stop and rests in \texttt{P\_SIL} for
400\,ms. The characters \texttt{d}/\texttt{D} are a special \emph{warm up} symbol:
the same EOF tone pair, held for 300\,ms instead of 144 (Section~\ref{sec:protocol}).

The player is also where the carrier sense lives. The decode side keeps a flag,
\emph{channel busy}, true whenever the slave carrier has been heard in the last 3
seconds. While the flag is up, \texttt{P\_IDLE} holds the queue and emits nothing;
and if the flag rises in the middle of a tone, \texttt{P\_TONE} stops the PWM at
once and yields. This is the half duplex rule of the protocol enforced at the
lowest level that can react quickly.

\subsection{Receive: poll, copy, decode}

\texttt{decode\_step} calls \texttt{read\_sdram\_tick}, the polling driver of
Section~\ref{sec:fetch}: it reads \texttt{fill\_cnt} (S0 offset \texttt{0x90}),
compares it with the last value seen and, only when it changed, copies the 512
bins from the BSRAM snapshot into a RAM array and raises a ready flag. Note that
the poll target is the counter, never the BSRAM itself: one bus read per loop
pass answers ``is there a new frame?'', and the 512 word copy is paid only when
the answer is yes (a counter also needs no clearing, Section~\ref{sec:fetch}). The decoder (\texttt{decoder.c}, the C port of
the VHDL decoder, Section~\ref{sec:accel}) then runs once per frame on that
array. Of its output, only the uppercase characters \texttt{A}--\texttt{C}, the
symbols riding the \emph{slave} carrier, are forwarded to the ESP32; the
lowercase ones are the master hearing its own transmission through its own
microphone, useful as a loopback check but not protocol input. Independently of
the full decode, a dedicated check on bin 27 alone (the slave carrier) refreshes
the channel busy timestamp, so carrier sense works even when no complete symbol
is recognisable.

\subsection{The health scan}

With ten slaves on the bus, ``the system stopped working'' has ten boring
explanations before any interesting one, so the firmware checks them itself.
Every 15 seconds \texttt{health\_tick} reads one \emph{safe} register from each
slave, applies a sanity check to the value (the fetch FSM status must show a
legal burst and index, a PWM data read must return its wired zero, a UART
status must have its reserved bits at zero), and assembles a bit mask, S0
through S8. Two slaves need indirection: the ADC SPI (S1) is never read
directly, because its data register only acks when a sample is ready and the
accelerator, which outranks the CPU, consumes every sample, so its health is
inferred from the accelerator's sample counter advancing; and the FFT chain is
judged the same way from the frame counter. The mask is OR-ed with the previous
scan before reporting, so a single dirty read cannot flag a healthy slave, and
the result goes out on the diagnostic channel as a \texttt{\$HLT} line that the
ESP32 forwards to the dashboard, which renders the per slave OK/KO lights; the
same mask feeds the SLAVES zone of the TFT. The failure mode is deliberately
informative: if a slave stops acking, the CPU hangs on that read, the
\texttt{\$HLT} heartbeat disappears, and the dashboard marks the system as
lost, while the first scan after boot prints a probe marker before each first
touch (\texttt{\$HP s2}, \texttt{\$HP s3}, \dots), so the last marker printed
names the slave that killed the CPU. That trick found the GPIO ack bug of
Section~\ref{sec:wbrules} in minutes.

\subsection{The diagnostic channel}

Printable diagnostics share the one character UART with the protocol traffic, so
they are framed: every diagnostic line starts with \texttt{\$} and ends with a
newline, and the ESP32 forwards anything between those two verbatim to the USB
console instead of feeding it to the protocol parser. Behind the settings debug
flag the firmware emits, on this channel, a once per second system line (loop
count, queue depth, the three hardware counters of Table~\ref{tab:s0map}), a per
decode line with the magnitudes of the five protocol bins, and every three
seconds a dump of all 512 bins from the snapshot. That last dump is how I
distinguished ``the FFT is not running'' from ``the FFT runs but the CPU reads
garbage'' more than once. Two lines ignore the flag because they are cheap and
load bearing: the one shot \texttt{\$NOR} boot summary and the 15 second
\texttt{\$HLT} heartbeat, which the ESP32 always forwards to the dashboard but
echoes on the USB console only when debug is on, so a quiet console stays
quiet.

% =====================================================================
\section{ESP32 firmware}
% =====================================================================

The ESP32 firmware is split into small modules. \texttt{uart\_reg} is a register
level driver for the ESP32 UART1 and UART2 peripherals, with no Arduino
HardwareSerial layer: UART2 (ESP32 pins 27/14) is the character link to the FPGA's
S6 UART, UART1 (pins 32/33) is the storage command link, which now lands on the
FPGA's S7 UART and is served by the soft core (the ESP32 never noticed the
hardware block behind it being swapped for firmware).
\texttt{flash\_link} performs the high level flash operations
against the FPGA controller, with retry and verify and a 60 record RAM cache for the
dashboard. \texttt{fpga\_uart\_link} is the codebook layer that turns decoded
characters into protocol patterns and drives the speaker through the FPGA.
\texttt{protocol} is the acoustic protocol state machine (presence, retries, abort,
registration). \texttt{web\_link} is the WebSocket client and JSON layer for the
dashboard; it also drives two status LEDs (Wi\,Fi up on GPIO 25, WebSocket up on
GPIO 26) at register level, through the \texttt{GPIO\_OUT\_W1TS/W1TC} set/clear
registers rather than \texttt{digitalWrite}. One power lesson belongs here: with
the radio at full transmit power the ESP32's current peaks tripped its own
brownout detector on a weak supply, so the firmware caps the Wi\,Fi transmit
power at 11\,dBm; the real cure is a solid 5\,V feed, but the cap keeps the board
alive on marginal ones.

\subsection{Register level UART}

The two UART links to the FPGA are driven by writing the UART peripheral registers
directly (the FIFO, CLKDIV, CONF0 and STATUS registers of UART1 and UART2), not
through HardwareSerial. The baud divider for 115200 from the 80\,MHz APB clock is
\[
\mathrm{int} = \left\lfloor \frac{80\,000\,000}{115200}\right\rfloor = 694,\qquad
\mathrm{frac} = \left\lfloor \frac{80\,000\,000\times 16}{115200}\right\rfloor \bmod 16 = 7,
\]
so the CLKDIV register is $694\,|\,(7\ll 20)=\texttt{0x7002B6}$, giving
$80\,000\,000/(694+7/16)=115{,}208$ baud, $0.007\%$ off. A free running 1\,MHz
hardware timer (TIMG0) gives microsecond timeouts for the UART reads without using
\texttt{micros()}.

\subsection{Flash link: retry and verify, and a defensive read}

When the dashboard sends a note, \texttt{web\_link} parses the JSON and calls
\texttt{flash\_log\_append}, which builds the 16 byte record, sends opcode
\texttt{L} plus the bytes over UART1, waits for the \texttt{K} acknowledgement,
reads the head back with \texttt{H}, reads the just written slot back with
\texttt{G}, and compares all 16 bytes; on a mismatch it retries, up to 5 times. The
record is also pushed into the RAM cache so the dashboard still shows it even if a
later NOR read misbehaves. The settings save works the same way against the 32 byte
sector. The settings read, \texttt{flash\_get\_settings}, validates before
returning: the magic byte must be \texttt{0xA5}, the retry count must be between 1
and 30, and the name must be printable ASCII; if any check fails it returns safe
defaults, so the dashboard never shows a garbled name or an absurd count even when
the NOR has a half written sector.

The settings blob grew with the display: alongside name, auto presence interval
and retry count, the dashboard now edits the debug flag, the three TFT theme
colours (sent to the browser as hex strings, stored as raw RGB bytes) and the
view flags that choose what the TFT zones show. The ESP32 is a pure courier for
these fields, it stores and forwards them without interpreting anything except
the debug flag, which it also applies to itself: the \texttt{\$HLT} health lines
from the FPGA always reach the dashboard, but they are echoed on the USB console
only when debug is on.

% =====================================================================
\section{The acoustic protocol}
\label{sec:protocol}
% =====================================================================

The protocol uses an alternating bit codebook. A pattern is two or three symbols,
with the rule that two consecutive symbols must differ; the decoder then triggers on
a \emph{change} of tone, which is easy to see, rather than on silence, which is hard
to measure over a noisy channel. There are six symbols: lowercase \texttt{a},
\texttt{b}, \texttt{c} (bit 0, bit 1, end of frame) ride the 2014\,Hz master carrier,
and uppercase \texttt{A}, \texttt{B}, \texttt{C} ride the 2472\,Hz slave carrier. The
master sends lowercase and the slave sends uppercase, so each side knows who spoke.

A message is identified by its first bit and its length, while the carrier gives the
direction; the same bits therefore mean two different messages on the two carriers.
Table~\ref{tab:codebook} is the whole codebook. Each pattern is framed by EOF tones
(\texttt{c} or \texttt{C}) at both ends.

\begin{table}[H]
\centering
\caption{The alternating bit codebook. A pattern (its first bit and length) plus the
carrier it rides on selects one message; the same pattern means different things on
the two carriers, because the carrier says who is speaking. On the master carrier the
data bits are the tones \texttt{a}/\texttt{b}, on the slave carrier \texttt{A}/\texttt{B}.}
\label{tab:codebook}
\begin{tabular}{llll}
\toprule
\textbf{Pattern} & \textbf{(first, length)} & \textbf{Master carrier (to slave)} & \textbf{Slave carrier (to master)} \\
\midrule
\texttt{01}  & (0, 2) & presence request   & presence: yes         \\
\texttt{10}  & (1, 2) & assign id ($=1$)   & presence: no          \\
\texttt{010} & (0, 3) & abort              & registration request  \\
\texttt{101} & (1, 3) & ---                & acknowledge           \\
\bottomrule
\end{tabular}
\end{table}

\subsection{On the air: warm up, redundancy, and detection by change}

What actually goes on the air for one message is more than the two or three data
symbols, because the channel is a cheap speaker, a cheap microphone, room
reverberation, and an automatic gain control on the receiving capsule. Three
field driven additions wrap the pattern (Figure~\ref{fig:tx-timeline}):

\begin{itemize}
\item a \emph{warm up} symbol opens every transmission: the EOF tone held for
      300\,ms instead of 144 (the ESP32 sends the special character \texttt{d},
      Section~\ref{sec:firmware}). Its job is to let the receiver's microphone
      AGC settle on a long, harmless tone; on the receiving side it is just an
      EOF, so it flushes an empty pattern and carries no data;
\item the EOF framing is sent as a \emph{burst of four} at both ends of the
      pattern, because single symbols get lost on this channel and an EOF is the
      one symbol whose loss breaks the framing rather than one bit;
\item every symbol is paced: 144\,ms of tone, 400\,ms of silence. The long gap is
      the symbol separator; with a short one, the reverberation tail of a tone
      runs into the next and two equal bits fuse into what sounds, and decodes,
      like one.
\end{itemize}

The receiver mirrors this with \emph{detection by change}: a decoded character is
accepted as a new symbol only if it differs from the last accepted one. This is
what the alternating codebook buys: inside a pattern two consecutive data bits
always differ, so a repeated character can only be the FPGA decoder reporting the
same tone across several FFT frames (each tone spans many 10.9\,ms frames) or
reverberation holding a tone up, and both are correctly collapsed into one
symbol. The four EOFs of the framing burst likewise collapse into a single EOF
event. A partial pattern with no new symbol for 2 seconds is discarded, so a
truncated frame cannot poison the next one.

\begin{figure}[H]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
  every node/.style={font=\scriptsize},
  tone/.style={draw, line width=0.7pt, fill=black!12, minimum height=0.55cm, anchor=south west},
  warm/.style={draw, line width=0.7pt, fill=black!25, minimum height=0.55cm, anchor=south west}
]
% time axis
\draw[-{Stealth}, line width=0.7pt] (0,0) -- (17.2,0) node[below left=1pt]{$t$};
% warm-up: 300 ms; tones 144 ms; gaps 400 ms. scale: 1 ms = 0.0105 cm -> tone .55, warm 1.16, gap .75
\def\toneW{0.55} \def\gapW{0.75} \def\warmW{1.16}
\node[warm, minimum width=\warmW cm] at (0.3,0.05) {d};
\def\xa{2.21} % 0.3+1.16+0.75
\foreach \i/\c in {0/c,1/c,2/c,3/c}{
  \node[tone, minimum width=\toneW cm] at ({\xa+\i*(\toneW+\gapW)},0.05) {\c};
}
\def\xb{7.41} % xa + 4*1.30
\node[tone, minimum width=\toneW cm] at (\xb,0.05) {a};
\node[tone, minimum width=\toneW cm] at ({\xb+\toneW+\gapW},0.05) {b};
\def\xc{10.01} % xb + 2*1.30
\foreach \i/\c in {0/c,1/c,2/c,3/c}{
  \node[tone, minimum width=\toneW cm] at ({\xc+\i*(\toneW+\gapW)},0.05) {\c};
}
% braces
\draw[decorate, decoration={brace, amplitude=4pt}] (0.3,0.78) -- (1.46,0.78)
  node[midway, above=4pt]{warm up 300 ms};
\draw[decorate, decoration={brace, amplitude=4pt}] (\xa,0.78) -- ({\xa+3*(\toneW+\gapW)+\toneW},0.78)
  node[midway, above=4pt]{EOF burst $\times4$};
\draw[decorate, decoration={brace, amplitude=4pt}] (\xb,0.78) -- ({\xb+\toneW+\gapW+\toneW},0.78)
  node[midway, above=4pt]{data \texttt{01}};
\draw[decorate, decoration={brace, amplitude=4pt}] (\xc,0.78) -- ({\xc+3*(\toneW+\gapW)+\toneW},0.78)
  node[midway, above=4pt]{EOF burst $\times4$};
% one tone + gap annotated (staggered so the labels do not collide)
\draw[<->, line width=0.5pt] (\xb,-0.25) -- ({\xb+\toneW},-0.25);
\node[below] at ({\xb+0.5*\toneW},-0.32) {144 ms};
\draw[<->, line width=0.5pt] ({\xb+\toneW},-0.7) -- ({\xb+\toneW+\gapW},-0.7);
\node[below] at ({\xb+\toneW+0.5*\gapW},-0.77) {400 ms};
\draw[line width=0.3pt, gray] ({\xb+\toneW},-0.25) -- ({\xb+\toneW},-0.7);
\end{tikzpicture}%
}
\caption{One transmission on the air (here the presence request, pattern
\texttt{01}). Every box is a two tone burst (carrier $+$ data tone); the darker,
longer first box is the warm up EOF that settles the receiver's AGC. The receiver
collapses each EOF burst into one EOF event by detection by change, so what
reaches the protocol layer is exactly \texttt{c\,0\,1\,c}.}
\label{fig:tx-timeline}
\end{figure}

\subsection{Half duplex, collisions and retries}

The channel is half duplex, so only one side transmits at a time, and the master
enforces it at two levels. On the soft core, the channel busy flag
(Section~\ref{sec:firmware}) holds the tone player whenever the slave carrier has
been heard in the last 3 seconds, and aborts a tone already in progress; this is
the fast reflex. On the ESP32, the link layer waits before \emph{every} byte it
sends for the channel to have been quiet for 1 second (giving up after 15
seconds, so a noisy decoder cannot block transmission forever); this spaces whole
patterns rather than single tones. If the master is emitting and the slave
carrier appears, the tone in progress stops and the master yields, so a collision
resolves to one talker.

Retries are handled by the ESP32, and they exist because the channel loses
symbols routinely, not exceptionally. A presence request with no answer within 8
seconds is repeated, up to a configurable number of attempts (default 6, stored
in the flash settings), after which the master gives up and tells the dashboard.
The registration SET is retransmitted on a slow randomised timer (30 to 40
seconds) until the acknowledgement arrives; the randomisation avoids the two
nodes locking into the same retry rhythm. Receiving is idempotent on both sides,
so a duplicate caused by a retry does no harm.

% =====================================================================
\section{Power supply}
% =====================================================================

Power enters as a single 12\,V supply rated 2\,A. A fuse and a series Schottky diode
protect against overcurrent and reverse polarity, then an LM2596 based DFRobot
DFR1202 buck module steps the 12\,V down to 3.3\,V at up to 0.5\,A for all the
digital parts (the FPGA board, the ESP32, the NOR flash, the ADC). Input and output
of the buck are decoupled with 100\,$\mu$F and 100\,nF capacitors. The 12\,V rail is
kept separate from the buck and feeds the two high current loads directly: the 6\,W
power LED and the speaker stage, each switched by its own low side MOSFET from a
logic pin. Keeping the LED and speaker on the raw 12\,V rather than on the 3.3\,V
buck keeps their switching current off the supply that feeds the sensitive ADC and
flash.

% =====================================================================
\section{What worked and what did not}
% =====================================================================

The parts that worked end to end are the bus and its address map, the SDRAM
controller once the refresh interval and the 1.54\,ns clock phase shift were set, the
512 point FFT with the parabolic peak decoder and its $\pm 3$ neighbourhood, the
alternating bit codebook (helped a lot by giving each direction its own short
sub codebook for the most repeated message, the presence request), the BSRAM
snapshot path that finally gave the CPU clean spectra, the NOR flash
controller after the decoupling and clock work, the WebSocket dashboard, and the
register level UART driver on the ESP32.

A few things were tried and dropped, or moved. The early design had a generic DMA
between the ADC and SDRAM with the CPU programming descriptors; it spent too many
CPU cycles per window, so I folded the gather and FFT launch into the streaming
accelerator. The first CPU read path to the spectrum, chasing the fetch FSM's
live burst buffer over the bus, lost the race and produced torn spectra; the
hardware BSRAM snapshot replaced it (Section~\ref{sec:fetch}). The FFT done
interrupt is wired to the core and the ISR plumbing exists, but polling won on
simplicity and the IRQ now only triggers hardware. The tone emitter and the tone
decoder both started as RTL state machines and were both ported to firmware once
the soft core was up; the RTL versions are still in the tree, disabled, as
references. The flash controller followed the same path last: once the bus rules
of Section~\ref{sec:wbrules} held, \texttt{flash\_ctrl} was retired and its
state machine re-emerged as \texttt{norflash.c}, behind the same ESP32 protocol.
Driving the UART pin to the ESP32 through a three way AND of banner,
hardware decoder and CPU UART broke the line in subtle ways, so the pin now
belongs to the CPU's UART alone. The
$\pm 1$ decoder neighbourhood was too strict and was widened to $\pm 3$. The plain
HardwareSerial on the ESP32 worked but did not show register level work, so I
replaced it with the direct register driver.

What still wobbles is mostly the wiring. The W25Q breakout is on Dupont jumpers, so
the SPI clock cannot realistically go above 633\,kHz; on a proper board with a
ground plane it would run far faster with no retries. The decoupling is 1 plus
10\,$\mu$F rather than the more orthodox 100\,nF plus 10\,$\mu$F, because that is
what I had on hand. And the very first status register read after power up sometimes
returns \texttt{0xFF} before the chip is ready, which makes the firmware print a
false "chip locked" warning that clears on the next read.

The migration that closed the loop is done: the flash protocol lives in the soft
core (S7 in, S8 out, Section~\ref{sec:flashctrl}), and the freed debug pins now
drive a TFT that shows the system state without a laptop
(Section~\ref{sec:display}). What remains is cleanup debt: the fetch FSM still
carries its bring up name \texttt{tst\_} in the RTL, and the retired blocks
(\texttt{flash\_ctrl}, the hardware decoder and emitter) still sit in the
project file as disabled references waiting to be dropped.

% =====================================================================
\section{Conclusions}
% =====================================================================

The master node is a working integration of a custom SoC on the Gowin GW2AR-18, an
acoustic receive pipeline from analog microphone through a 12 bit ADC, a 512 point
FFT and a tone decoder, a NOR flash store whose protocol runs in the soft core, a
TFT dashboard drawn by the same core, and an ESP32 that runs my
AUSP protocol on top and bridges it to a web dashboard. The single most useful
lesson was that for the mixed signal parts the datasheet number is where the problem
starts, not where it ends: the simple relation $\Delta V = I\,t/C$ explained a
"flash write succeeds but the data is not there" bug that had looked like a firmware
fault for days.

\appendix
\section{Quick reference}

\begin{table}[H]
\centering
\caption{Key numbers used in the design.}
\resizebox{\textwidth}{!}{%
\begin{tabular}{ll}
\toprule
Bus / CPU clock & 40.5 MHz \\
SDRAM clock & 40.5 MHz; die on phase shifted \texttt{clkoutp} ($+1.54$ ns, $22.5^\circ$) \\
SDRAM refresh & one AUTO REFRESH every 7.81 $\mu$s \\
SDRAM traffic & bursts of 26 words, one row per burst; $20\times26$ per frame \\
ADC sample rate & 46.875 kHz (40.5 MHz / 864) \\
FFT & 512 point, bin width 91.6 Hz; one frame every 10.92 ms (91.6 Hz) \\
Tone bins & 22, 27, 32, 39, 50 (2014 / 2472 / 2930 / 3571 / 4577 Hz) \\
Spectrum snapshot & BSRAM at S0 \texttt{0x4800\_1000}; poll \texttt{fill\_cnt} at \texttt{0x90} \\
Tone pacing & 144 ms tone + 400 ms silence; warm up EOF 300 ms \\
PWM carrier & speaker 158.2 kHz (40.5 MHz / 256); LED 105.5 kHz (27 MHz / 256) \\
NOR SPI clock & 633 kHz (divide by 64), via slave S8 \\
ADC SPI clock & 750 kHz (divide by 54) \\
Display SPI clock & 6.75 MHz (divide by 6, mode 3, TX only) \\
UART baud & 115200 (divide by 352); RX FIFO 16 deep \\
W25Q64 JEDEC ID & EF 40 17 \\
Sector erase / page program & up to 400 ms / up to 3 ms \\
Flash timeouts (firmware) & 2 ms / byte, 700 ms WIP poll, 200 ms / payload byte \\
Decoupling at the flash & 1 $\mu$F + 10 $\mu$F \\
Log record / settings & 16 B / 32 B v2 (marker \texttt{0x5A}), 512 ring slots \\
Health heartbeat & \texttt{\$HLT} every 15 s; TFT zone refresh every 1.5 s \\
Protocol retries & presence: every 8 s, 6 tries (configurable); SET: 30--40 s \\
Carrier sense & soft core: 3 s hold; ESP32: 1 s quiet per byte, 15 s cap \\
\bottomrule
\end{tabular}%
}
\end{table}

\end{document}
